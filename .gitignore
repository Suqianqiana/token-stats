# ============================================================
# .gitignore  —  token-stats
# 仓库定位：以「源码 + 文档」为唯一真相源。
# 一切可再生的、历史的、体积大的中间产物都不进版本库（磁盘保留）。
# 2026-09-19 第53轮整理：历史物已集中到 _archive/，故原先逐条列的
# 素材/审查页排除规则全部收敛为一条 /_archive/。
# ============================================================

# --- 归档区：历史物集中存放，磁盘保留、不进版本库 ---
/_archive/

# --- 质检/窗口配置 JSON（工具链生成物，非源码）---
/assets/pet_*_qc.json
/assets/pet_*_windows.json

# --- 字节码与缓存 ---
__pycache__/
*.pyc

# --- 运行日志 ---
*.log

# --- 调试与临时产物 ---
/_*.png
/assets/_*.png
/_*.txt
*.bak
*.bak.*

# --- 本地快捷方式 / 误生成的重复副本 ---
*.lnk
/ (*).*

# --- 打包中间产物 ---
/build/
/dist/
*.spec
!TokenStats.spec

# ============================================================
# 2026-09-19 第56轮：推送到 GitHub 前补齐的三类排除
# ============================================================

# --- ① 敏感文件：运行时凭据与本地数据（**绝不入库**）---
# 这些文件本来只存在于本机数据目录
# (~/.workbuddy/plugins/data/token-usage-stats/)，列在这里是"防误放"：
# 万一有人把它们拷进项目目录，也不会被误提交上去。
sn_token.json
sn_login_state.json
sn_account.json
sn_autosync.json
sn_autosync.invalid.json
sn_points_sync.json
machine.json
settings.json
stats.json
peers/
*_ledger*.json
*.pem
*.key
*.p12
.env
.env.*

# --- ② 依赖目录（本机用 .workbuddy 内的 venv；此处防误建）---
/.venv/
/venv/
/env/
/node_modules/
*.egg-info/
.eggs/

# --- ③ 测试/检查缓存 ---
/.pytest_cache/
/.mypy_cache/
/.ruff_cache/
.coverage
htmlcov/

# --- ④ 编辑器与操作系统 ---
/.idea/
/.vscode/
Thumbs.db
desktop.ini
.DS_Store
