# V3 补充素材清单（2026-09-17）

源图两张（浅浅猫提供，均为**已是透明背景**的 RGBA PNG，四角 alpha=0）：

| 前缀 | 源文件 | 尺寸 | 内容 |
|---|---|---|---|
| **A** | `ChatGPT Image 2026年9月17日 03_19_52 (1).png` | 1536×1024 | 2 行 × 6 列 = 12 个姿势 |
| **B** | `ChatGPT Image 2026年9月17日 03_19_53 (2).png` | 1535×1024 | 2 行 × 6 列 = 12 个姿势 |

**编号规则**：行优先（左→右、上→下）。`A01`~`A06` = 图1 第 1 行，`A07`~`A12` = 图1 第 2 行；
`B01`~`B06` = 图2 第 1 行，`B07`~`B12` = 图2 第 2 行。

**处理方式**：零抠图（源已是透明 PNG）；**连通域切分**（不用竖线切 —— 图1 第 2 行有 6 个角色但
只有 4 个"干净空隙"，竖切必裁断）；裁剪后**保留原始 alpha**，未做任何颜色/透明度改动。
帧尺寸 229~266 × 318~422，与现有 `pet_v3r` 帧（200~365 × 260~407）同量级。

产物：`assets/pet_v3_add/A01.png … B12.png` + `manifest.json`（含每帧裁剪框，可回溯源图坐标）
检视页：`docs/pet_v3_add_review.png`（洋红底、按真实 alpha 合成）

---

## 一、逐帧识别与建议场景

> 「场景」指 V3 现有的 7 组：`idle` 待机 / `sleep` 睡觉 / `wake` 睡醒 / `drag` 被拖拽 /
> `click` 点击反馈 / `sidle` 特殊待机（小剧场）/ `pat` 摸摸头。

### A 组（第 1 张图）

| 编号 | 位置 | 我看到的内容 | 建议场景 | 把握 |
|---|---|---|---|---|
| A01 | 行1-1 | 举起右手**挥手打招呼**，眨眼微笑 | click / idle | 高 |
| A02 | 行1-2 | 双手交叠在身前，安静微笑站立 | **idle** | 高 |
| A03 | 行1-3 | **双手举起欢呼**、闭眼大笑 | **click** | 高 |
| A04 | 行1-4 | 双手**捧着粉色爱心**在胸前，害羞笑 | click / pat | 高 |
| A05 | 行1-5 | **手持扫帚**，身边有闪光 | **sidle** | 高 |
| A06 | 行1-6 | **抱着一本蓝皮书** | **sidle** | 高 |
| A07 | 行2-1 | **提着裙摆微微低头**，眼神向下（有点小委屈） | idle | 中 |
| A08 | 行2-2 | **坐在地上抱着鲸鱼玩偶、在吃饼干** | **sidle** | 高 |
| A09 | 行2-3 | 一手叉腰一手举起，眨眼，旁边有星星 | click | 中 |
| A10 | 行2-4 | **双手举起欢呼跳跃**（腿抬起） | **click** | 高 |
| A11 | 行2-5 | 双手举起**拿着星星**，闭眼笑 | pat / click | 高 |
| A12 | 行2-6 | **提着裙摆行屈膝礼**（谢幕姿势） | click | 高 |

### B 组（第 2 张图）

| 编号 | 位置 | 我看到的内容 | 建议场景 | 把握 |
|---|---|---|---|---|
| B01 | 行1-1 | 双手交叠身前，站立微笑 | **idle** | 高 |
| B02 | 行1-2 | 双手交叠，**微微抬头睁大眼睛**（好奇/惊讶） | idle | 中 |
| B03 | 行1-3 | 双手**捧着一颗黄色星星**，闭眼笑 | pat / click | 高 |
| B04 | 行1-4 | **安静坐着**，双手放在腿上 | idle（坐姿）/ wake | 中 |
| B05 | 行1-5 | **揉眼睛 / 擦眼角**，闭眼委屈状 | **wake** ← 刚睡醒揉眼 | 高 |
| B06 | 行1-6 | **手持扫帚**，一手叉腰 | **sidle** | 高 |
| B07 | 行2-1 | 一手**端着茶杯** | **sidle** | 高 |
| B08 | 行2-2 | 双手**抱着一本书** | **sidle** | 高 |
| B09 | 行2-3 | **双手合十**在胸前（拜托/感谢） | pat / click | 高 |
| B10 | 行2-4 | 双手**比心**，旁边有粉色爱心 | pat / click | 高 |
| B11 | 行2-5 | **抱着蓝白鲸鱼抱枕**，闭眼笑 | **sidle / pat** | 高 |
| B12 | 行2-6 | **坐在地上单手撑地、另一手挥手** | click / idle | 中 |

---

## 二、按场景汇总的建议（待确认）

| 场景 | 现有帧数 | 建议补充 | 补后 | 说明 |
|---|---|---|---|---|
| `idle` 待机 | 5 | A02 / B01 / B02（+ A07 可选） | 5→8 | 都是"站立/小动作"，不改变"默认静止为主"的节奏 |
| `wake` 睡醒 | 2 | **B05**（揉眼睛）+ B04（坐起过渡） | 2→4 | 目前只有"坐起睁眼/伸懒腰"，揉眼是最贴睡醒的动作 |
| `click` 点击反馈 | 4 | A01 / A03 / A10 / A12（+ A04 A09） | 4→8 | 全部是"被点一下之后的开心反应" |
| `sidle` 特殊待机 | 3 | **A05 A06 A08 B06 B07 B08 B11** | 3→10 | 扫帚/书/茶杯/饼干/鲸鱼都是小剧场素材，这组最能吃下新素材 |
| `pat` 摸摸头 | 3 | A04 / A11 / B03 / B09 / B10 | 3→8 | 爱心/星星/合十/比心都是"被摸头后的撒娇" |
| `sleep` 睡觉 | 3 | — | 3 | **新素材里没有躺/趴睡姿势**，不补 |
| `drag` 被拖拽 | 4 | — | 4 | **新素材里没有被拎起/悬空姿势**，不补 |

**总计**：上表是"我的建议"，**最终以浅猫的分配为准**（见第五节）。

> 说明：这一节的建议保留作对照。她 09-17 拍板的最终分配与我的建议有几处不同
> （例如 `B05` 揉眼睛归了特殊待机、`A10`/`B03`/`B12` 归摸摸头），**一律按她的来**。

## 五、最终分配（浅浅猫 2026-09-17 拍板 · 已入库）

**A07 本轮不使用**（放回备选库，未入库）。其余 23 帧分配如下：

| 编号 | 动作 | 入库为 |
| --- | --- | --- |
| A05 | 手持扫帚 | `sidle/sidle_04.png` |
| A06 | 抱蓝皮书 | `sidle/sidle_05.png` |
| A08 | 坐地抱鲸鱼吃饼干 | `sidle/sidle_06.png` |
| A09 | 叉腰眨眼 | `sidle/sidle_07.png` |
| B05 | 揉眼睛 | `sidle/sidle_08.png` |
| B06 | 手持扫帚（叉腰） | `sidle/sidle_09.png` |
| B07 | 端茶杯 | `sidle/sidle_10.png` |
| B08 | 抱书 | `sidle/sidle_11.png` |
| B11 | 抱鲸鱼抱枕 | `sidle/sidle_12.png` |
| A02 | 双手交叠站立 | `idle/idle_06.png` |
| A04 | 捧爱心 | `idle/idle_07.png` |
| B04 | 安静坐着 | `idle/idle_08.png` |
| A01 | 挥手打招呼 | `click/click_03.png` |
| A03 | 双手欢呼 | `click/click_04.png` |
| A11 | 举星星 | `click/click_05.png` |
| A12 | 提裙行礼 | `click/click_06.png` |
| B01 | 双手交叠站立 | `click/click_07.png` |
| B02 | 抬头睁大眼 | `click/click_08.png` |
| B09 | 双手合十 | `click/click_09.png` |
| B10 | 比心 | `click/click_10.png` |
| A10 | 欢呼跳跃 | `pat/pat_04.png` |
| B03 | 捧星星 | `pat/pat_05.png` |
| B12 | 坐地挥手 | `pat/pat_06.png` |

> `click` 一栏的编号是 **2026-09-17 退役 `click_03`/`click_04` 并重排后**的结果，
> 与首次入库时不同（详见第八节）；原始对应关系在 `installed_map.json` 里始终可查。

**入库后各场景帧数**（自检：每帧只含 1 个本体连通域，全部 OK）：

| 场景 | 原 | 首次入库 | 退役 2 帧后 |
| --- | --- | --- | --- |
| idle | 5 | **8** | 8 |
| sleep | 3 | 3 | 3 |
| wake | 2 | 2 | 2 |
| drag | 4 | 4 | 4 |
| click | 4 | 12 | **10** |
| sidle | 3 | **12** | 12 |
| pat | 3 | **6** | 6 |
| **合计** | 24 | 47 | **45** |

- 入库脚本 `tools/install_pet_v3_add.py`（**只新增、绝不覆盖**；同名文件已存在会跳过并提示）
- 映射表 `assets/pet_v3_add/installed_map.json`（编号重排后自动同步，含 `renumber_log`）
- 全量检视页 `docs/pet_v3r_review.png`、单场景放大页 `tools/review_pet_scene.py <场景>`
  （如 `docs/pet_click_review.png`，标签带 `←A01` 形式的来源标注）
- 检视图与素材类图片**一律放工作空间 `docs/`，不再往桌面放副本**（浅猫 09-17 要求）
- `click`/`pat`/`sidle` 三组在代码里是**随机取一个帧**播放（`random.randrange(len(pool))`），
  所以加/减帧只改变变化量、不会变成"顺序循环"；`idle` 走 `_pet_idle_seq(n)`，第 0 帧仍是默认待机，
  新增的 3 帧只会被"偶尔随机插入"。

### 待你确认的一处
- **B05（揉眼睛）**：你写的是"A05060809,B0605 可以当做特殊待机"，我按字面把 `B0605` 理解成
  **B05 + B06** 都进特殊待机（`sidle_08` = B05）。若它其实该归"摸摸头"，说一声即可
  （换目录一条命令，很快）。

## 六、顺带：桌宠点击分区（浅猫 09-17 的新想法，已实现）

- **点角色顶部 1/3** → 摸摸头（`pat`）；**点底部 2/3** → 普通点击互动（`click`）
- **任意位置双击** → 打开统计面板（原行为保留）
- 分界取"**实际绘制区域**"的 1/3，不是窗口的 1/3 —— 矮宽的帧（如躺姿）只画在窗口下半部分，
  按窗口算会把"角色头顶以上的空白"也判成摸头。实测 v3 素材下分界线落在窗口高度 **35%** 处，
  贴近你要的 1/3。
- 右键菜单项与悬浮提示同步更新文案（"点头部 1/3 也行"）。
- 回归新增 12 项断言（分区边界、无绘制区回退、双击打开面板、47 帧加载、新帧连通域自检）→ **117 项全绿**。

---

## 三、为什么"不能直接切分"

| 判据 | 图1 | 图2 |
|---|---|---|
| 角色数（连通域面积≥20000） | 12 | 12 |
| 宽度比（≥1.45 = 两个角色粘连） | 0.90~1.06（无粘连） | 0.96~1.14（无粘连） |
| 第 1 行：角色数 / 干净空隙数 | 6 / 5 → 竖切安全 | 6 / 5 → 竖切安全 |
| 第 2 行：角色数 / 干净空隙数 | **6 / 4 → 竖切必裁断** | 5 / 4 → 勉强安全 |

图1 第 2 行就是"不能竖切"的实证：角色在 x 方向投影互相重叠，任何竖线都会从角色身上切过去。
所以本次一律走**连通域切分**：以每个角色自己的连通域为单位取 bbox，框内属于**其它角色**的
连通域像素置 0（保留 `label==0` 的抗锯齿边缘），自检"每帧恰好 1 个本体连通域"——24 帧全部 OK。

## 四、关于透明背景的两点核查（避免无谓改动）

- 图1 的透明区是"带颜色的 RGB + alpha=0"（RGB 标准差 ~29）；**现有 V3/V4 帧完全一样**
  （非零 RGB 占 100%），属正常现象，alpha=0 不会被绘制 → 不动它。
- 图2 超出角色 4px 的半透明像素**100% 是 alpha=1~2**（0.4% 不透明度，肉眼不可见）→ 默认不清。
  若她不放心，切分脚本带 `--denoise` 可一键清掉（实测可清 ~4 万像素），默认关闭。

## 八、退役记录：`click_03` / `click_04`（浅猫 2026-09-17 要求）

浅猫看过放大版检视页后说"点击效果里第三第四张图效果不理想，移除到备选素材库"。
**指认已确认**：按 `click` 场景内序号数的第 3、4 张 = `click_03`（眨眼竖手指）+ `click_04`（惊讶张嘴），
**这两个是 V3 原有的老帧**，不是 09-17 新入库的素材（当时已把两种数法摆出来请她确认）。

| 项 | 内容 |
| --- | --- |
| 退役帧 | `click_03.png`（眨眼竖手指）、`click_04.png`（惊讶张嘴） |
| 去向 | `assets/pet_alt/click/old_click_03.png`、`old_click_04.png`（**文件原样保留，不是删除**） |
| 索引 | `assets/pet_alt/index.json`（记录原路径、退役时间、原因） |
| 后果 | `click` 12 → **10 帧**；剩余帧**重排为连续编号**，全量 47 → **45 帧** |
| 工具 | `tools/retire_pet_frames.py <场景> <编号...> [--dry] [--reason=...]` |

**编号变化（重排前后）**——以后提到"click 第 N 张"请以新编号为准：

| 现编号 | 原编号 | 来源 |
| --- | --- | --- |
| `click_01` | click_01 | 原有 |
| `click_02` | click_02 | 原有 |
| `click_03` | ~~click_05~~ | A01 挥手打招呼 |
| `click_04` | ~~click_06~~ | A03 双手欢呼 |
| `click_05` | ~~click_07~~ | A11 举星星 |
| `click_06` | ~~click_08~~ | A12 提裙行礼 |
| `click_07` | ~~click_09~~ | B01 双手交叠 |
| `click_08` | ~~click_10~~ | B02 抬头睁大眼 |
| `click_09` | ~~click_11~~ | B09 双手合十 |
| `click_10` | ~~click_12~~ | B10 比心 |

**两个设计上的注意点（都是实做时踩到的）**：
1. **退役 = 移动，不是删除**。备选库里的文件带 `old_` 前缀保留原编号，随时可取回；
   索引里有 `was` 字段记录它原来住在哪。
2. **重排编号必须两步走**（先改 `.rtmp` 临时名再改目标名），否则 `click_05→click_03` 会撞上
   还没让位的 `click_03`。重排后**同名文件≠同内容**，所以"退役帧是否真的不在主库"这类校验
   **必须比内容哈希，不能比文件名**——回归断言一开始就是按文件名写的，直接被这条现实打回。
