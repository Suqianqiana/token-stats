# Token 审计卡片 协作进度（项目文档）

> 本文档记录「Token 审计卡片」（WorkBuddy / DSH Token 用量统计 + 商汤积分额度浮窗）从立项开发到当前版本落地的完整历程、架构与目录结构，并约定本地 Git 版本管理规范。
> 管理方式参照 WBCacheCleaner《写作进度》的「每轮改动先更新本文档、再提交」习惯，但**仓库仅本地管理，不上传任何云端**。

---

## 一、项目概述

### 1.1 这是什么
Token 审计卡片是一款**面向 Windows 的常驻桌面统计浮窗**：屏幕右下角悬浮球常驻（置顶、可拖动、位置记忆），单击弹出无边框圆角卡片窗口（液态玻璃主题、深浅色），内可切换三个数据源 **WorkBuddy / DSH / 商汤积分**。纯 Python 实现，UI 层 PySide6（Qt 6.11.1），扫描引擎纯标准库，无后端服务、不依赖系统 Python 环境（运行于托管 venv `C:\Users\a3564\.workbuddy\binaries\python\envs\pyside6\`）。

### 1.2 核心功能与定位
- **WB / DSH Token 用量统计**：2×2 汇总卡（合计 Token / 缓存命中率 / 输入·输出 / 会话·请求）、今日条、按模型明细列表（彩点+比例条+占比+命中率+请求数）、每日堆叠柱状图、GitHub 风活跃热力图；时间区间切换（今日/近7天/近30天/全部）。
- **商汤积分额度页**（完全独立组件，不复用旧控件）：通用积分池（紫）/ Flash-Lite 专属积分池（橙）/ 活动固定积分（1:1 返赠 30 天有效）三张大卡；进度条按官方口径（本周余额·周 / 5h 窗口剩余·占比 / 官方重置时间）。
- **商汤自动同步**（第45轮重构，详见文末本轮记录）：**真自动**为主路径——本地直连官网 `pool-usage` 接口（**0.2~0.8s**，token 是标准 JWT，载荷自带 exp，可本地预判有效期）；凭据失效时用系统 Edge（Playwright 持久登录态，必要时自动代填账号密码）静默续签（**≈5.6s**）。保留「F12 抓包 Copy as cURL → 粘贴」作为回退通道（域名必须为商汤，否则视为无效配置并归档）。运行时 300s 频控 + 5 分钟周期刷新 + 进入续期窗口（剩余 <15min）自动提前换证；失败回退本地估算并明确展示原因与「上次成功 HH:MM」。
- **桌宠形态**：悬浮球可切换为 DeepSeek 娘桌宠（46 帧素材动画：待机眨眼小动作/入睡/被拖拽/单击表情反馈/随机小剧场/摸摸头）；双击打开统计面板；素材缺失自动回退经典悬浮球。
- **定位**：个人开发者的 Token 消耗与积分额度「看得清」工具——数据全部来自本地会话记录与用户自行抓包的接口，无任何上传。

### 1.3 技术架构
```
Python 3.13 + PySide6（Qt 6.11.1）
  ├─ card_app.py          UI 层（~3100 行）：
  │   ├─ 主题系统          THEMES(light/dark) + GLASS_ALPHA → refresh_palette() 全局调色板
  │   │                    qrgba()/qname() 输出 QSS；深浅色/玻璃即时切换，settings.json 持久化
  │   ├─ CardWindow        无边框置顶卡片（FixedWidth 664 / 高度自适应≤1000 / place_right 顶部对齐）
  │   │                    数据源三路 Tab + 时间区间 + 后台扫描调度（RefreshBridge 源快照
  │   │                    + _pending_refresh 补刷，根治跨源渲染竞争）
  │   ├─ WB/DSH 内容区     StatCard 2×2 汇总 / TableHeader+ModelRow 明细（斑马纹/hover tooltip）
  │   │                    StackedBarChart 堆叠柱图 / HeatMap 竖排月历热力图 / ChartTip 悬浮卡
  │   ├─ 商汤页 SNQuotaPage  SNPoolCard×2（本周余额 21pt + 5h 进度条 + 周额度行）
  │   │                    SNPromoCard（竖排堆叠防截断）/ SNSyncPanel 内嵌同步面板
  │   ├─ 商汤同步          第45轮链路：_sn_edge_path(Edge 多候选+注册表动态发现)
  │   │                    → _sn_load_token + _sn_token_exp(本地 JWT 过期预判)
  │   │                    → _sn_direct_fetch(token 直连, 8s 超时/2 次重试/10061 绕代理)
  │   │                    → 失效则 _sn_playwright_login_and_fetch(事件驱动等响应,
  │   │                       commit 级导航, try/finally 关浏览器, 必要时自动代填账号密码)
  │   │                    → 回退 load_sn_autosync/cURL(_http_json + 零指纹结构探测)
  │   │                    → sn_autosync_fetch(300s TTL + 续期窗口提前换证) → load_sn_stats
  │   │                    · 全链路写 sn_cred.log（走了哪条路/耗时/结果，供事后定位）
  │   └─ BallWindow        悬浮球（矢量闪电/黑白玻璃随主题）+ 右键菜单 + 自启开关
  ├─ scanner.py            数据引擎（纯标准库）：遍历 ~/.workbuddy/projects/**/*.jsonl，
  │                        提取 providerData.usage（camelCase），(mtime,size) 游标增量聚合，
  │                        写 cache.json + stats.json（每模型/每日/会话统计）
  ├─ peer_store.py         多机数据源引擎（纯标准库，2026-09-19 第50轮）：导出本机快照
  │                        （WB+DSH 全量）→ 导入别机（按机器名存档 peers/<machine>.json，
  │                        同名覆盖不翻倍）→ merge_machines 合并本机+多别机（按来源/按机拆分）
  ├─ test_switch_race.py   offscreen 回归测试（289 项断言，QT_QPA_PLATFORM=offscreen；
  │                        商汤凭据 / settings / peers 存档一律改到临时目录，绝不碰真实配置）
  └─ 启动器                启动统计悬浮球.vbs（wscript 静默拉起 pythonw，主推）
                           start_card.bat / 启动统计悬浮球.bat（bat 别名）/ enable_autostart.bat
```
- **三条铁律级教训**（勿重蹈）：
  1. Qt 自绘圆角窗口上，任何系统级全窗口模糊（DWM SystemBackdrop / AccentPolicy）都带矩形边界——玻璃感只能靠**纯 Qt 分层透明度 + 高光描边**自绘（V6~V10 五轮试错定论）。
  2. **UI 主线程永不执行网络/重 IO**——一律后台线程 + Signal 回主线程（V3 刷新卡死、09-10 保存卡死崩溃两次验证）。
  3. **⚠️ 绝不能生成 `__pycache__/card_app.cpython-313.pyc`**（2026-09-16 定位）：火绒把它判为
     `Trojan/Python.ShellLoader.am`（rid `D78FACEAE47CB091`，置信度 100%）**删除文件 + 结束进程**，
     当天实测被杀了 **12 次**（08:23:44~09:06:36），触发者全是"跑测试/诊断的那条 python 命令"——
     表现为**命令莫名返回空、进程莫名消失**，极易误判成代码 bug。
     **规矩**：① 任何 `import card_app` 的脚本开头必须 `sys.dont_write_bytecode = True`；
     ② 跑测试用 `python -B`；③ **不要用 `python -m py_compile`**（它会写 pyc），改用
     `python tools/syntax_check.py`；④ 火绒**信任区**加入项目目录 + `.workbuddy` 工具链目录（根治）。

### 1.4 模块职责
| 文件 | 职责 |
|------|------|
| `card_app.py` | 主程序（PySide6）。主题系统、**统一视觉基座（`make_btn`/`style_btn` 按钮工厂、`GlassDialog` 自绘弹窗、`NavButton` 纯自绘导航）**、CardWindow 四源卡片窗口、WB/DSH 汇总与图表、商汤积分页（SNQuotaPage/SNPoolCard/SNPromoCard/SNSyncPanel）、多机页（MultiMachinePage/MachineRow/StatRankRow/SrcBadge/MachineChip）、cURL 自动同步（parse_curl/_http_json/零指纹/频控）、悬浮球、自启管理 |
| `scanner.py` | 数据引擎（纯标准库）。jsonl 增量扫描聚合：签名游标缓存、每模型/每日/会话聚合、stats.json 原子写入；可独立 `python scanner.py --force` 调试 |
| `peer_store.py` | 多机数据源引擎（纯标准库，第50轮）。导出包构建/校验（`token-stats.peer/1`）、机器名安全化、peers 存档读写（导入/覆盖/删除）、`merge_machines` 多机合并（按来源 bySource + 按机 machines） |
| `test_switch_race.py` | offscreen 回归测试 289 项：切页 race、同步数学（auto 精确透传）、cURL bash/cmd 解析、指纹/零指纹、真实接口 fixture、单列渲染、窗口顶部对齐、10061 直连重试、多机导出/导入/合并、视觉基座（按钮工厂/自绘弹窗/导航自绘/主题联动/排行条）；开头备份/结尾恢复用户真实配置，商汤/settings/peers 全隔离到临时目录 |
| `启动统计悬浮球.vbs` | 静默启动器（wscript 无窗口；bat 双击必闪窗，故弃用 bat 做日常入口） |
| `start_card.bat` / `启动统计悬浮球.bat` | ASCII 启动器（内容相同，前者为规范名；供快捷方式/自启指向） |
| `enable_autostart.bat` | 注册启动文件夹快捷方式（程序内右键「开机自启动」为另一路径，vbs 优先、注册表兜底） |
| `legacy/` | 归档：旧网页版方案 `app.py`（tkinter 悬浮球+本地 HTTP 面板）+ `panel.html`（不再被任何入口引用，磁盘保留不入库） |
| `docs/` | `assets/previews/` 界面预览图（offscreen 渲染，中文方框为环境缺字体，实机正常） |

---

## 二、演进历程（按时间 / 版本）

- **2026-08-25 项目启动 · 网页悬浮球方案（v1）**
  - `app.py`（tkinter 悬浮球 + ThreadingHTTPServer:27182）+ `panel.html`（深色统计面板）+ `scanner.py`（jsonl 增量扫描聚合）。
  - 全量扫描 184 文件 / 25534 条 usage 约 6.2s，增量 <0.5s；timestamp 兼容 ISO 字符串与 Unix 毫秒。

- **2026-08-25 V2 重设计 · PySide6 卡片窗口**
  - 用户反馈网页方案不便 → 新建 `card_app.py`：无边框圆角置顶卡片窗口 + 悬浮球；照参考截图实现 2×2 汇总、模型彩条明细、堆叠柱图、热力图；scanner 增加 today/sessions 统计。

- **2026-08-25 V3 精修 · 刷新卡死根治（第一次「线程纪律」验证）**
  - 根因：工作线程直接调 `QTimer.singleShot`（非线程安全）→ 改 `RefreshBridge(done Signal)` 信号槽回主线程；明细表改固定列；加 –/✕ 按钮（关闭=hide）；会话数按日 sessionId 去重。

- **2026-08-25 V4~V5 打磨 · 自启与菜单**
  - ChartTip 单例悬浮卡、右键菜单美化（圆角/悬停/图标）、6px 圆角滚动条；`enable_autostart.bat` 自启；启动器双命名（中文 bat + ASCII bat）。

- **2026-08-25 V6~V10 玻璃模式五轮试错（重要教训沉淀）**
  - V6 右侧停靠 `place_right` + DWM Acrylic；V7 用户批评「整窗透明无层级」→ 分层玻璃（THEMES+GLASS_ALPHA，文字恒不透明）+ 深色模式；V8 玻璃方框修复尝试（DWM ROUND）；V9 AccentPolicy + 启动优化（球先出、卡片后建、数据延后一拍）；V10 用户实测仍有方框 → **定论：自绘圆角窗口上任何系统级全窗口模糊都带矩形边界，玻璃=纯 Qt 分层半透明，此路封死**。

- **2026-08-25 V11~V13 启动器与热力图**
  - 自启闪窗：bat 必闪 cmd 窗口 → 弃用 bat 换 VBS（wscript 静默）；自启双路径（Startup vbs 优先、HKCU Run 注册表兜底）；VBS 引号翻倍转义 bug 修复；热力图真日历对齐（行/列坐标系统一）+ 竖排月历。

- **2026-08-25 V14~V17 窗口缩窄拉锯 · QLabel 撑爆最小宽度真因**
  - 多轮「缩窗口无效」真因：超长文本 QLabel 的 sizeHint 被布局当最小宽度，全窗 min 锁死 856px → `setWordWrap+setSizePolicy(Ignored)` 解除；`setFixedWidth(664)` 锁死；比例条精确 140px；另发现多实例陷阱（关闭=hide，旧实例残留）。

- **2026-08-25 V18~V20 DSH 数据源**
  - 新增 DSH 源：以 `cost-meter/ledger.json` 为**唯一权威源**（usage-stats-cache.json 按 session 重复累计会翻倍 6×，弃用）；补齐请求数/花费；输入口径统一为总输入（命中+未命中）；悬浮球改黑白玻璃矢量闪电。

- **2026-08-26 V21~V23 悬浮提示与暗色可读性**
  - ChartTip 重写（两列对齐/随主题换肤）、ModelRow 整行 hover 详情卡；暗色次级文字三轮提亮（TEXT2/TEXT3 直至 #d6dae0/#bcc1c8）。

- **2026-08-26 商汤积分池 Phase 3**
  - `load_sn_stats()` 返回 pools 三卡结构（通用/Flash-Lite/活动占位）；移除校准方案；官方口径上限 60000/5h、600000/周（公测期固定值）。

- **2026-08-28 商汤页独立重设计 + 切页卡顿修复**
  - 完全独立 `SNQuotaPage`（不复用 StatCard/ModelRow），官网风格卡；旧内容收进 `content_old` 容器与 SN 页物理隔离；切页卡顿根因=主线程同步扫描 → `_sn_cache` 缓存直渲。

- **2026-09-08 商汤页三项修复**
  - ① 手动同步 `SnSyncDialog`（官网数字为基准+本地扣减，窗口翻滚回退估算）；② SN 事件磁盘缓存 `sn_events_cache.json`：冷启动 14.4s → 命中 0.159s（~90×）；③ **切页跨源渲染竞争**根治：`RefreshBridge.done` 携带发起源快照，源不匹配只入缓存不渲染 + `_pending_refresh` 补刷。

- **2026-09-08 cURL 抓包自动同步（真实接口适配）**
  - 用户抓到真实接口 `GET platform.sensenova.cn/lite/console/v1/tokenplan/pool-usage`（Bearer JWT 仅 3h + Cookie；数字全为字符串）。`parse_curl` 兼容 bash/cmd（`^` 续行、`^"` 转义、`-b` Cookie）；指纹自学习 `_find_value_paths` + `_enrich_paths` 补全 reset_at/grant 路径；auto 值**精确透传**不走浮点往返；300s 进程内频控；401 自动回退手动值。

- **2026-09-08 零指纹配置 + 三反馈修复**
  - 保存后立即完整抓取（promo/reset 不再等 5 分钟）；⟳ 强制刷新绕过 TTL；**零指纹结构探测** `_detect_paths_by_structure`（按 pools[].pool_type/name 认字段）→ 配置只需贴 cURL，数字框全空。测试扩至 48 项。

- **2026-09-10 UI 重构（内嵌同步面板 + 单列大卡）**
  - `SnSyncDialog` 二级对话框删除 → `SNSyncPanel` 内嵌商汤页底部；积分卡改单列每卡独占一行；`SNPromoCard` 竖排堆叠防右侧截断（总量余额 21pt 独占行）、池卡底部靠左；`place_right` 固定基准高度 880 → 各页顶部对齐；测试扩至 52 项。

- **2026-09-10 项目文档与目录整理**
  - 弃用 `app.py`/`panel.html` 归档 `legacy/`；预览图归档 `docs/assets/previews/`；初建 `docs/ARCHITECTURE.md` 与新版 `README.md`。

- **2026-09-10 WinError 10061 修复（网络层）**
  - 用户报「接口调用失败 10061 积极拒绝」：根因=系统代理指向的本机端口无进程监听（代理软件未运行），urllib 默认读系统代理；与商汤服务器无关。`_http_json` 重构：默认 opener 失败且错误含 10061 → `ProxyHandler({})` **自动绕过系统代理直连重试**。

- **2026-09-10 保存流程全面异步化（第二次「线程纪律」验证，修崩溃）**
  - 用户报「配置一直停在测试中然后崩溃」：根因=保存的网络请求在 UI 主线程同步执行（代理超时下挂 10~20s → 未响应 → 崩溃），且保存后重算 `load_sn_stats(force=True)` 又在主线程再发一次请求。
  - 修复：`SNSyncPanel` 拆 `_do_test_and_save`（测试→零指纹→保存→抓取写回退）放后台线程，结果经 `save_finished(bool,str)` 信号回主线程；测试期间禁用按钮；`_on_sn_sync_saved` 改走 `refresh(force=True)` 后台通道；worker 异常兜底。测试扩至 **54 项**并新增慢网络冒烟。

- **2026-09-10 协作规范升级**
  - 按 WBCacheCleaner《写作进度》格式重建本主协作文档（整合原 docs/ARCHITECTURE.md）；启用本地 Git 管理（`.gitignore` + 「第N轮」提交约定，仅本地不上云）。文档由用户定名《协作进度.md》。

- **2026-09-10 商汤页瘦身：移除手动同步残留 + 切页高度修复（本轮）**
  - **移除手动同步全链路**（用户反馈：已不需要，切商汤页仍有手动检测步骤）：删 `SN_SYNC_FILE`/`load_sn_sync`/`save_sn_sync`/`clear_sn_sync` 与 `sn_points_sync.json`；`load_sn_stats` 去掉 manual 回退分支（auto 失败即本地估算）；`_do_test_and_save` 保存后不再写手动回退文件；`set_status`/`_on_scan_done` 删「最后已知值/已同步」残留分支；同步面板「清除配置」不再清手动文件。自动同步成为唯一积分来源。
  - **切页高度问题最终处理：完全回退，不再干预（见下「方案迭代」）**：原问题=切页布局中间态 min 冲到 1267+ 把窗口短暂撑高（用户原话"会变高但之后会恢复"，属可自愈的视觉问题）。所有强制修复（SetNoConstraint / render 内同步收口 / 延后收口）都在实机上引入更严重的**永久卡死**，最终**完全回退 `render()` 至第 9 轮原样**。
  - **方案迭代（两轮全推翻 → 完全回退）**：第一版用 `SetNoConstraint` 禁用布局约束 → 实机切页全卡；第二版在 `render()` 内同步收口（`setMinimumHeight(0)`+`resize(664,880)`+`activate()`），第三版改 `QTimer.singleShot(0, self._settle_height)` 延后收口，两者均致实机日期切换永久卡死（resize-布局事件风暴饿死 paint）。**最终全部撤回：`render()`/`_switch_range` 恢复第 9 轮逐字原样，高度问题不再强制干预（短暂变高自愈，不卡死）**。
  - **第 11 轮实机定位（用户日志 + 实机复测）**：实机日志显示 `_switch_range`/`render()` 全链路执行完成但 UI 永久卡死、点⟳刷新才恢复；连 `QTimer.singleShot(0, self._settle_height)` 延后收口仍卡。**确定根因=自第 10 轮起的任何高度干预代码（收口/约束/resize 强制）在真实窗口系统引发 resize-布局事件风暴饿死 paint**。最终修复：**删除全部高度干预（`_settle_height`/收口/`SetNoConstraint`/`setMinimumHeight(0)`），`render()` 与 `_switch_range` 恢复第 9 轮逐字原样**。验证：WB/DSH/商汤全切换毫秒级响应、高度干预零残留、回归 64 项全绿。
  - **悬浮球右键新增「🔄 一键重启」**（用户需求）：`_restart_app()` 用 pythonw 静默拉起新实例（DETACHED|NEW_PROCESS_GROUP），300ms 后 `os._exit(0)`；新实例经 `_kill_stale_instance()` 兜底杀旧 PID，保证单实例且加载最新代码。
  - 回归测试同步瘦身：删手动同步数学/数据层两组用例，第 2 节改为「auto 精确透传 + 未配置字段回退估算」；401 用例改断「无同步值 + 本地估算」；断言数 68 → **64 项全绿**。

- **2026-09-11 商汤页清理「本地估算」残留文案（第 13 轮）**
  - 用户反馈"商汤页面里还是有根据本地数据估算的残留" → 清理 5 处用户可见文案（剩余估算字样仅在代码注释）：① 状态行 921「未配置自动同步 — 余额为本地估算」→「未配置自动同步」；② 清除提示 929「已清除同步配置 (回到本地估算)。」→「已清除同步配置。」；③ 页脚 1078「…未配置同步时余额为本地调用记录估算…」→「最大额度为官方公开的公测期固定值；余额以官网控制台为准，配置自动同步后显示实时额度。」；④ tooltip 2462「本地估算 + 官网同步」→「自动同步」；⑤ subtitle mode 2605「本地估算」→「未配置自动同步」。

- **2026-09-11 商汤页切页闪旧根治（第 14 轮，进行中）**
  - **用户反馈**："刚切到商汤页的时候，会是旧的页面，然后一瞬间才变成现在的页面"（非文案问题，是**切页闪旧/闪空态**：offscreen 复现确认切到商汤时 sn_page 立即显示但 pools=0 空态，200ms 后后台刷新才渲染真积分卡）。
  - **根因**：_sn_cache 只在用户"切走时"更新（_on_scan_done 的 elif src==sn 分支），**停留在商汤页刷新完成时（if src==self.source 分支）不更新 _sn_cache** → 切回商汤页用过期/空缓存，先渲染空态再刷新变新页。
  - **修复**：card_app.py _on_scan_done（2591-2594 行）在 if src==self.source 分支内新增 if src == "sn": self._sn_cache = s（注释：商汤刷新完成也同步缓存，避免切走再切回用过期缓存闪旧）。
  - 验证：首次切商汤 pools=0 空态（正常）→ 停留刷新后缓存更新 pools=1 → 切走再切回直接用缓存 pools=1 不再闪空态 → 往返稳定；全量回归 64 项全绿。

- **2026-09-10 桌宠形态（DeepSeek 娘帧动画，本轮）**
  - 用户提供单图合并素材（DeepSeek 娘桌宠素材包，6 组动作绘于一张 1536×1024 图），实现悬浮球桌宠化：
  - **素材拆分管线**（产物入库 `assets/pet/`，46 帧 1.3MB）：腐蚀 2px 去区块边框细线 → 连通域检测角色帧 → 行内列投影精分粘连帧 → 边缘 flood-fill 去白底转透明（保留角色内部白色）→ 小连通域清除白色碎屑（4565 处）→ 内容 bbox 裁剪（播放时底部对齐居中）。special「10 读书」帧与大立绘粘连无法干净拆出而弃用（09 帧已含读书画面），成品 idle 7 / sleep 6 / drag 6 / click 6 / other 12 / special 9。
  - **BallWindow 桌宠化**：双形态切换（右键「🐳 桌宠形态」勾选，settings.json 持久化）；帧动画状态机——idle 待机（手排 38 步序列：站立为基准，眨眼高频、伸懒腰/撩头/开心低频）、无交互 60s 入睡（sleep 慢循环）、拖拽播 drag 帧、单击随机 click 表情（眨眼/惊讶/开心/生气/头顶星星/爱心）、idle 期 40~90s 随机 other 小剧场、右键「🤗 摸摸头」触发 special；一次性动作播完自动回待机；素材缺失自动回退经典悬浮球。
  - **交互调整（桌宠模式）**：单击=互动反馈动画，**双击=打开统计卡片**；拖拽移动不变。
  - 回归测试扩至 **67 项**（新增第 8 节：素材加载/形态切换/状态机全路径/渲染/回退）；预览 `docs/assets/previews/pet_mode.png`。

- **2026-09-10 桌宠修补轮：抠图修复 + 动画节奏重调（用户两项反馈）**
  - **反馈①「衣服/面部/发饰的白色被扣掉」**：原 flood-fill 阈值过宽（min>=222 & diff<=26）+ 全帧小白块清除误伤内部白色。重写去白算法（**实心主体掩码法**）：去噪 fg（<228、组件≥40px）→ `fill_holes` 得保护掩码 M（角色轮廓+内部白区整体保护）→ **8-连通背景判定**：与帧边界连通的背景透明；封闭白腔分 size 处理（≥150px=衣饰白保留 / <150px=发丝间隙碎片透明）。中间版本踩坑记录：closing 外扩 6px 致白边块（弃）、JPEG 蚊噪暗点阻断泛洪致白块残留（改"非实体即可传播"解决）、4-连通把发丝对角细缝判为封闭孔（改 8-连通解决）。
  - **反馈②「动得太频繁、一下子循环所有帧，太生硬」**：idle 由固定 38 步循环改为**随机生成段式待机**（`_pet_idle_seq()`）——静止 4~10s + 眨眼一下，45% 概率夹带微笑/扭头/撩头，22% 概率伸懒腰/开心；一段播完重新随机生成。小剧场间隔 40~90s → **90~240s**、动作减为 3 帧；入睡序列改「长静止睡姿 + 缓慢换姿势」（720ms）；摸摸头/单击表情缩短。总体：静止为主、偶动一下。
  - 回归 67 项全绿；深/浅双底拼图逐版本目检（v6→v9 共 4 轮迭代）。

- **2026-09-10 桌宠修补轮 2：背景残留根治 + 单实例治理（用户二次反馈：外部白背景没扣掉 / 节奏调了没变化）**
  - **背景白根因**：v9 的尺寸启发式（封闭腔 ≥150px 保留）把「角色环抱出的开阔背景腔」（手臂圈出的空腔、睡眠蜷圈、被枕区）当衣服保留了——这些腔可达数千 px。**正确特征是色彩而非尺寸**：采样腔内像素统计，背景腔纯白平坦（平均饱和度 3.0~4.5），衣服/头饰白带蓝调阴影纹理（饱和度 6.0~13.3）。新算法：封闭腔按**平均饱和度 ≥5.5 保留 / <5.5 透明**，46 帧重生成，深/浅双底目检通过（浅底即原素材观感）。
  - **节奏「没变化」根因 = 旧实例驻留**：卡片 ✕ 是隐藏不退出（V17 教训），用户双击启动器看到的仍是旧代码窗口。新增 `_kill_stale_instance()` 单实例治理：main() 启动时按 `ball.pid` 定位旧进程 TerminateProcess，保证**双击启动器永远加载最新代码**；节奏同时再拉长（静止 6~17s、小动作 35%、大动作 15%、小剧场 150~420s）。实测待机序列平均 14.8s，其中动作帧仅 ~2.5% 时长。
  - 回归 67 项全绿。

- **2026-09-10 桌宠修补轮 3：抓到节奏失效的真正根因（paintEvent 取帧 bug）+ 单实例治理**
  - **用户三次反馈「还是旧的循环」的真正元凶**：`paintEvent` 取帧误写 `frames[self._si % len(frames)]`——把"序列索引"直接当"帧号"用，**根本没读取 `_seq` 帧号序列**！无论节奏逻辑怎么调，实际展示永远是 idle 7 帧每 240ms 顺序轮播。第 3/4 轮改的节奏逻辑全是"正确但没接上线"。
  - **修复**：取帧改为 `_seq[_si % len(_seq)] % len(frames)`（跟随序列帧号）。offscreen 实测：模拟 120 tick（28.8s）仅 6 次换帧/3 个动作帧（静止 33 tick→眨眼→静止 40 tick→眨眼→静止 20 tick→微笑），睡眠慢速换姿 ✓。回归新增"实际展示帧跟随 _seq"断言（**68 项**全绿），防再犯。
  - 右键菜单顶部显示 `APP_BUILD` 版本号（当前 `v9-fix 2026-09-10 05:00`），用户可一眼确认运行的是哪版代码。
  - 已替用户启动新实例验证（PID 守护 + 进程存活确认）。

- **2026-09-10 桌宠修补轮 4（用户四项反馈）**
  - **发饰/枕头/衣服被扣坏**：饱和度分级对"纯白扁平"的枕头和部分发饰失效 → 放弃自动分级，改回「封闭白腔全保留」+ **sleep 组专用闭运算掩码**（保住被子/枕头整块）+ 边界连通组件按**内切半径**分级（≥8px 厚白区=角色贴边部分保留 / <8px 薄环带=背景删）→ 发饰、枕头、衣服全部完整。
  - **click 底部数字残留**：click 行 y1 616→590 收紧（同 idle 行标签教训），底部数字已净。
  - **拖拽仍全帧循环**：drag 状态由全帧循环改为**随机保持一个被拎姿势**（`[i]*10`）。
  - **sleep_03 移除**（用户不要了），sleep 组剩 5 帧。
  - click_01（贴窗口边+棋盘格装饰）单独用整块保留模式，并删除不接触蓝发的白色孤立块；素材装饰棋盘格保留（素材自带）。
  - 回归 68 项全绿。

- **2026-09-10 桌宠修补轮 5：click_01 弃用**
  - 用户决定放弃 click_01（贴窗口边+棋盘格装饰，问题最多的帧），click 组改用 5 帧（惊讶/开心/生气/头顶星星/爱心）。回归 68 项全绿（帧数断言 idle7/sleep5/drag6/click5/other12/special9）。

- **2026-09-10 桌宠修补轮 6：背景白真正根治（发现素材背景是棋盘格纹理）+ 双判定封腔**
  - **关键发现**：原素材的"透明背景"在 JPG 化时被渲染成了**棋盘格纹理**（PS 透明底的经典灰白格），并非纯白——此前所有"白底残留/白块"的反复，根源都是没意识到这一点。
  - **v16 最终算法**：M = fill_holes(去噪 fg)；**M 外（背景+棋盘格）全透明**；M 内封闭腔双判定删背景腔——A. 通透性：腔在「仅深色实体(<200)为障碍」语义下与边界连通比例 >30%（细发丝围栏腔）→删；B. 开阔度：腔 ≥1000px 且内切半径 ≥20（手臂圈等环抱开阔腔）→删；其余封闭白腔=描边完整包围的衣服/发饰白→保留。
  - 46 帧重生成（click_01/sleep_03 维持弃用，实际 44 帧生效）；深底逐组目检：idle/sleep(被子+鲸鱼完整)/drag/click(5帧)/other/special 全部背景干净、内部白色完整。
  - 教训：处理"白底 JPG 抠图"先确认背景到底是纯白还是棋盘格纹理——两者的自动清理策略完全不同。
  - 回归 68 项全绿；已重启实例（PID 5356）。

- **2026-09-10 桌宠修补轮 7：抠图方法重做（量化质检 + 根治「棋盘格 vs 内部白」二律背反）**
  - **用户批评（成立）**：前三轮一直在「→ 有棋盘格背景 → 修 → 内部白被扣 → 修 → 又回到棋盘格」之间打转；且我靠缩略拼图目视判断，**评估方式本身有缺陷**（22 帧内部破洞被漏检、破脸帧当成合格），也未把素材交用户确认。
  - **破局点**：原素材"透明区"在 JPG 里是**棋盘格纹理**（白格 + 浅灰格，均为低饱和中性色），不是纯白。据此改为**泛洪 + 面积兜底**（v20）：
    1. 从帧边界对「中性浅色」泛洪 → 背景含棋盘格整体清除；**角色内部白色被描边挡住，泛洪进不去，天然保留**（发饰/衣服/枕头不再被误删）；
    2. 渗入兜底：闭运算封缝后按面积分级——**封闭孔 <600px 填回**（修复被误删的内部白）、**≥600px 保持透明**（手臂圈出的环抱背景腔）；阈值取自实测分布（衣饰白腔 ≤551px vs 背景腔 ≥747px）；
    3. 边缘收白：轮廓最外 2px 内近白像素降为半透明（6774 px），消除深色桌面白晕。
  - **新增量化质检**（取代目视）：内部孔洞 px / 灰白残留 px（彩色装饰不计）/ 羽化边 px；对比：**孔洞 22 帧 → 0 帧**（v16 的漏检问题由此暴露）。
  - **按用户决定**：sleep 组仅保留 01/02（其余 4 帧移除）；special 组发饰被切问题经重新处理修复（9 帧完整）；click_01、special_10 维持弃用。
  - 最终 **41 帧**（idle7/sleep2/drag6/click5/other12/special9），质检 **41/41 通过**（孔洞 0、灰白残留 0）；成果报告 `docs/pet_qa_report.html`（逐帧深/浅双底 + 指标）。回归 68 项全绿。

### 2026-09-12 浅浅猫 × WorkBuddy（第15轮：新版高清素材 + 按角色轮廓分割 + 双版本切换）
- **新素材**（用户提供新版高清图，深色闭合描边）：按**角色自身连通域轮廓**分割（不再矩形裁切）——
  `fill_holes(闭合描边)` 直接得到完整轮廓（内部白色天然保留）；粘连帧（被拖拽 2/3 尾巴搭接）用
  **实心轮廓腐蚀断桥 + 测地 BFS 归属**分开，交错重叠不互相切；发丝间隙用「小封闭腔 + ≈面板底色 + 贴近轮廓」三条件清除；边缘 4 倍超采样 + 中值滤波 + 孤立碎屑清除。
- 新素材 **13 帧**：`assets/pet_v2/`（idle4/sleep2/drag3/click2/special2，无 other 组）；拆帧脚本归档 `tools/split_pet_v2_assets.py`。
- **双素材版本**：`PET_THEMES`（v1 经典 44 帧 6 组 / v2 新版 13 帧 5 组），右键菜单「🎨 素材版本」切换并记忆；
  动画状态机按各版本实际帧数自适应（待机序列生成器 `_pet_idle_seq(n)`、v2 无 other 组自动跳过小剧场）。
- **窗口治理**：清理 2 个残留 card_app 实例（PID 16336/16140，9/11 启动——「窗口不断弹出」的元凶）。
- 回归测试扩至 **76 项全绿**（新增：v1/v2 帧数、序列不越界、双版本切换、v2 无 other 跳过小剧场）。
- 新增 `docs/assets/previews/pet_mode_v2*.png` 预览。

- **2026-09-13 第25轮：横版大改版 V8.2（跨会话合并记录）**
  - **背景**：用户交付横版重构源码（990×610 固定尺寸 + 左导航 + 桌宠智能吸附）。第15~24轮（git 05c67a9 及之前）为桌宠 v2 素材抠图精修，已并入。
  - **横版新架构**：固定 990×610——左半区模型用量排行榜，右半区堆叠柱状图+热力日历；左 190px 导航栏（WB/DSH/商汤 + 刷新 + 深色/液态玻璃开关）；商汤页横排 2 池卡 + 活动积分条 + 内嵌同步面板，固定尺寸根治高度忽变。
  - **桌宠智能吸附展开**：popup_from 按桌宠所在屏半区决定左右展开、纵向居中、限制在可用区内。
  - **修复交付版严重缺陷**：① [cite: 1] Gemini 引用残留×3（acc_since[pid][cite: 1] 等）→ 删残留恢复正确加法，修复配置自动同步后点商汤页必崩（NameError: cite）致命 bug；② 补回 _find_value_paths（新版删了旧版指纹函数）；③ promo 兜底与金额精度恢复（.1f → .3f+rstrip）；④ 第13轮文案回退修复（同步面板「未配置自动同步」/页脚定稿/subtitle mode）；⑤ 桌宠 click/special seq 恢复 3 帧（新版 len2 致回归 FAIL）；⑥ SNSyncPanel 补 _sync_test 钩子。
  - **回归适配**：切页断言改新版接口（_switch_nav(btn_nav_*) / stack.currentIndex()==0 / pools_layout 横排 2 卡 + promo_bar）；商汤卡片 3 单列→2 横排+活动条；顶部对齐→固定尺寸恒定；报错文案匹配新版。**ALL 77 CHECKS PASSED**。
  - 备份：card_app.v24.worktree.20260913.bak.py（磁盘保留）。

- **2026-09-13 第26轮：商汤官网风格额度组件现代化重构**
  - 用户交付商汤页精细重构代码并认可第25轮修复；审核后替换：SNProgressBar（圆角渐变双色条）、SNPoolCard（双栏平衡布局 + Flash-Lite/通用池彩标徽章）、SNSyncPanel（胶囊状态徽标 status_pill + 引导文案）、SNQuotaPage（promo 优雅横幅 + 固定高度对齐）。
  - **审核修复 4 项**：① 第13轮「本地估算」文案再次回退（foot_lbl「未配置则根据本地日志估算」、set_status「本地统计估算中」）→ 恢复定稿「最大额度为官方公开的公测期固定值；余额以官网控制台为准，配置自动同步后显示实时额度。」与「未配置自动同步」；② _sync_test 测试钩子丢失 → 补回（回归 §3/§6 依赖同步执行）；③ 错误文案「未识别到 pools 结构」缺「商汤」二字（测试断言匹配）→ 补全；④ status_lbl 改名 status_pill → 加兼容别名 self.status_lbl = self.status_pill。
  - 验证：py_compile OK、三态状态文本断言 OK、冒烟渲染（池卡2/余额千分位/暗色主题/空态）OK、**ALL 77 CHECKS PASSED**。备份 card_app.py.pre_sn_refactor.bak。

- **2026-09-13 第27轮：商汤页布局再优化——纵向多层级卡片（解决遮挡/挤压）+ 保留上版精华**
  - 用户反馈布局仍需优化（左右双栏挤压/信息重叠）→ 采纳新布局：**SNPoolCard 改纵向多层级**（顶栏胶囊徽标+池名+适用模型 / 中层周可用额度大数字+周重置时间 / 底层 5h 通栏满宽进度条+重置独占一行），信息两端通栏舒展，彻底消除挤压；promo 横幅高度收至 36。
  - **择优保留上一版**：① SNProgressBar 恢复渐变填充（lighter 高光）；② 同步面板保留**胶囊状态徽标**（用户明确要求：三态自动同步绿/异常红/未配置灰，带背景色圆角）。
  - **审核修复 2 项**：① SNSyncPanel.__init__ 漏初始化 _status_msg（apply_theme 立即调用 setText → AttributeError）→ 补 `self._status_msg = "未配置自动同步"`；② 文案再次核对：foot_lbl「数据均以控制台实际调用与配额为准」（无估算字样）、错误文案保持「未识别到商汤 pools 结构」（测试断言一致）。
  - 验证：py_compile OK、实例化 OK、冒烟（纵向卡片属性齐全/胶囊三态/promo 千分位/暗色/空态）OK、**ALL 77 CHECKS PASSED**。备份 card_app.py.pre_v27.bak。

- **2026-09-13 第28轮：商汤对称仪表 + promo 双行 + 柱状图缩放平移**
  - **用户交付的下载文件核查未采纳**（仍是旧版：无三项改进、带 [cite: 1] 致命残留、文案回退、缺 _sync_test）→ 基于真实需求在当前最新版上直接实现。
  - **SNPoolCard 周/5h 完全对称双仪表**：上下两组同构——指标标签 + 数值/占比百分比 + 圆角渐变进度条 + 专属重置时间，通栏舒展无挤压。
  - **赠送积分双行卡片**：第一行 🎁活动固定积分 + Flash-Lite 1:1 消费返赠 · 30天有效 规则；第二行 可用总额（Consolas 13pt 高亮）+ 最近到期。
  - **StackedBarChart 视口引擎**：滚轮以鼠标指向日期为锚点缩放（上滚放大/下滚缩小，Y 轴随视口峰值自适应）；按住左键拖拽平移历史日期；鼠标悬停明细悬浮卡保留。
  - 审核修复：滚轮缩放方向首版写反（上滚反变宽）→ 修正 factor=1/1.25；视口索引用例边界钳制。
  - 验证：py_compile OK、冒烟（对称双仪表属性/promo 双行/缩放 zoom=True/平移 pan=True）OK、**ALL 77 CHECKS PASSED**。备份 card_app.py.pre_v28_chart.bak。

- **2026-09-13 第29轮：iOS 27 液态玻璃效果（LiquidGlassFrame/GlassPodFrame 纯 Qt 自绘）**
  - 用户交付新版本（gemini-code-1789263929612.py，2987 行）增加 iOS 27 玻璃效果：主底板 LiquidGlassFrame（微折射双层对角渐变 + 动态光随鼠动流体高光斑 + 侧栏分型槽线 + 双层菲涅尔镜面边缘 + 顶部镜面光弧），StatCard/SNPoolCard/SNSyncPanel/promo/table/chart/heat 卡片全部改 GlassPodFrame 悬浮透镜子卡。
  - **核查结论：整体健康**——纯 Qt 自绘玻璃（零系统级模糊 API，符合 V6~V10 铁律）、第28轮成果全保留（对称双仪表/promo 双行/柱状图滚轮缩放+平移）、_sync_test/status_lbl/_status_msg 初始化/胶囊徽标/无 cite 残留/无「本地估算」文案。
  - **仅补 2 处兼容**：① `_find_value_paths`（新版删了，测试 §3/§5 依赖）补回；② `promo_card` 加 `promo_bar` 兼容别名（测试 §6 断言 promo_bar is not None）。
  - 验证：py_compile OK、实例化 OK、玻璃渲染/光斑随鼠动/移出清除 OK、对称仪表/glass pod/promo 双行/胶囊/缩放平移冒烟 OK、**ALL 77 CHECKS PASSED**。备份 card_app.v28.20260913.bak.py。

- **2026-09-13 第30轮：修复桌宠模式重启后显示「大悬浮球」bug**
  - **用户反馈**：桌宠模式下重启，启动后显示一个很大的旧悬浮窗球，需再点一次桌宠模式才恢复。
  - **根因**：BallWindow.__init__ 末尾桌宠分支只调 _pet_apply(True)（改尺寸/状态），**未同步 self.pet = True**（初始 False，仅 set_pet 会设）→ paintEvent 的 `if self.pet and ...` 分支不成立画被拉大的悬浮球；_pet_tick 也因 `if not self.pet: return` 不推进动画。
  - **修复**：该分支内补 `self.pet = True` 后再 _pet_apply(True)。
  - 验证：桌宠模式启动 pet=True/尺寸 118x148/state idle/渲染走桌宠分支；悬浮球模式仍 54px；set_pet 双向切换正常；**ALL 77 CHECKS PASSED**。

- **2026-09-13 第31轮：虹彩液态玻璃 v8.6（尺寸模式/透明度等级/桌面宠物适配）**
  - 用户交付新版本（gemini-code-1789267837363.py，3213 行，APP_BUILD v8.6-liquid-iridescent）：新增 `create_iridescent_gradient` 虹彩渐变（主底板/子卡/悬浮球边缘统一使用）、**窗口尺寸模式**（default 990 / large 1180，`apply_size_mode`）、**玻璃透明度三档**（crystal/balanced/frosted，`set_glass_transparency`）、各组件 `apply_size` 随字号模式缩放。
  - **核查修复 2 项**：① `[cite: 1]` Gemini 引用残留又回退（1513/1518 行 acc_since[pid][cite: 1] 等，商汤同步计算 NameError 必崩）→ 删除恢复正确加法；② `_find_value_paths` 缺失（测试 §3/§5 依赖）→ 补回。
  - **确认保留**：桌宠重启修复（self.pet = True）、promo_bar 兼容别名、_sync_test、胶囊状态徽标、无「本地估算」文案、玻璃纯 Qt 自绘（零系统 API）。
  - 验证：py_compile OK、尺寸模式 990↔1180、透明度三档切换、虹彩渐变渲染、桌宠修复保留、**ALL 77 CHECKS PASSED**。备份 card_app.v30.20260913.bak.py。

- **2026-09-13 第32轮：v8.7 晶透水滴玻璃预设（右键菜单全面细化）**
  - 用户交付小修复版（gemini-code-1789272115572.py，3237 行，APP_BUILD v8.7-crystal-glass）：玻璃通透度预设置入主题（crystal 晶透水滴/balanced 标准液态/frosted 柔和微透，带名称与描述）；右键菜单新增「🔮 玻璃通透度」三档（💎💧🌫️）与「📐 窗口尺寸」（标准 990×610 / 舒适大窗口 1180×730）；组件尺寸经 curr_metric() 集中缩放。
  - **核查修复 2 项（老熟人回归）**：① `[cite: 1]` 残留×2（1529/1534 行）删除；② `_find_value_paths` 缺失补回。
  - **确认保留**：桌宠重启修复（self.pet=True）、promo_bar 兼容别名、_sync_test、胶囊状态徽标、无「本地估算」文案、玻璃纯 Qt 自绘。
  - 验证：py_compile OK、预设/尺寸 990↔1180/透明度三档/桌宠修复冒烟 OK、**ALL 77 CHECKS PASSED**。备份 card_app.v31.20260913.bak.py。

- **2026-09-13 第33轮：启动/重启提速 + 桌宠渲染流畅性优化**
  - **剖析定位**：启动耗时大头=import card_app 260ms（PySide6 固有）+ BallWindow 41ms（桌宠素材 77ms 全量加载）。
  - **优化1 桌宠素材懒加载**：非桌宠模式启动不加载素材（BallWindow 41ms→1.4ms，-97%）；首次开启桌宠/切主题时才经 `_load_frames()` 加载（含 v2→v1 回退与缓存）。
  - **优化2 重启加速**：`_restart_app` 的 300ms→150ms 退出延迟（新实例 `_kill_stale_instance` 兜底杀旧 PID，150ms 足够防双击抖动又明显更快）。
  - **优化3 静止帧跳过重绘**：`_pet_tick` 帧号未变化时不再 `update()`——桌宠待机/入睡静止期重绘 -96%（120 tick 120 次→5 次），CPU/绘制开销大降。
  - 验证：懒加载冒烟（非桌宠 1.4ms/开启桌宠 130ms 一次/切主题缓存命中）、静止帧 5/120、**ALL 77 CHECKS PASSED**。

- **2026-09-13 第34轮：修复桌宠开关从右键菜单消失（懒加载副作用）**
  - **用户反馈**：桌宠开关不见了。
  - **根因**：第33轮懒加载后非桌宠模式启动时 `pet_frames=None`，而右键菜单桌宠项的显示条件是 `if self.pet_frames is not None:` → 整个「🐳 桌宠形态」选项消失。
  - **修复**：显示条件改为 `PET_THEMES[self.pet_theme].dir` 目录存在（`os.path.isdir`，轻量不触发加载）；点击开启时才经 `set_pet(True)` 懒加载素材。
  - 验证：非桌宠启动(素材未加载)时菜单开关可见、开启后素材正常加载、**ALL 77 CHECKS PASSED**。

- **2026-09-13 第35轮：商汤积分「真自动」凭证刷新（Playwright 持久登录态，彻底告别 3h 手动抓包）**
  - **背景**：商汤积分此前是「半自动」——cURL 抓包凭证 3h 过期后需手动 F12 重新获取。用户需求：真·自动获取凭证刷新。
  - **调研结论**：账号密码登录需 AES 加密 + 首次图形/短信验证码，纯 HTTP 模拟登录不可行；登录采用 OAuth2 OIDC（Ory Hydra，scope 含 offline_access）。选定**方案 A：Playwright 持久登录态**（复用系统 Edge，免下载 Chromium）。
  - **逆向成果**：积分接口 = `/lite/console/v1/tokenplan/pool-usage`（需 Authorization JWT，由 console 页 JS 自动带）；登录流程 = `nova/login` → `consent` 授权 → `oauth2/token`；账号密码表单（用户名框 `请设置用户名` / 密码框 `请输入密码` / 登录按钮）。
  - **实现**：① 新增 `sn_login.py`（全自动登录：自动填账号密码点登录，无验证码）+ `商汤自动登录.bat` 启动器 + `sn_autologin_fetch.py`（独立抓取工具）；账号密码存本机 `sn_account.json`（不进 git）。② `card_app.py` 新增 `sn_playwright_fetch()`：Playwright 无头加载 `sn_login_state.json` 登录态 → 响应监听捕获 pool-usage → 复用 `_detect_paths_by_structure` 零指纹解析成 values → 登录态过期自动重登。③ `sn_autosync_fetch` 优先 Playwright（`SN_USE_PLAYWRIGHT` 开关，默认 True），失败回退 cURL 旧方案。
  - **测试兼容**：`test_switch_race.py` 开头加 `ca.SN_USE_PLAYWRIGHT = False`（回归走 cURL mock 路径，不触发真实网络）。
  - **验证**：sn_playwright_fetch 解析出完整 values（general/flash/promo 全字段）；load_sn_stats 走 Playwright 真自动返回 sync_src=auto 完整积分数据；**ALL 77 CHECKS PASSED**。

- **2026-09-13 第36轮：商汤同步提速 28 倍 + 进度反馈 + 账号密码卡片**
  - **用户反馈**：真自动已可用但速度太慢（同步也慢）、获取凭据时更新按钮灰色像卡住、希望商汤页加账号密码输入卡片（换账号/开源友好）。
  - **速度优化（核心）**：逆向定位 access_token 存于 localStorage（`access_token` 键，JWT）。新增 `_sn_direct_fetch()` 用 token urllib 直连 pool-usage，**同步从 10~28 秒 → 200~600ms（提速约 28 倍）**；token 缓存到 `sn_token.json`（快路径），401 时才回退 Playwright 重登刷新。
  - **进度反馈**：SNSyncPanel 新增 `set_syncing()` 第四态（橙色「⟳ 正在同步…」），CardWindow.refresh 的 SN 源同步时调用，消除更新按钮灰色无反馈的卡顿感。
  - **账号密码卡片**：新增 `SNAccountCard` 类（🔐 账号密码 + 显示/隐藏密码 + 保存按钮），挂载到商汤页；输入后持久化到 `sn_account.json`，保存时清除旧 token 缓存触发新账号重登（方便换账号）。
  - 修复：`_sn_direct_fetch` 的 `opener.open(context=...)` 参数错误 → 改 `urlopen(req, context=ctx)`。
  - 验证：token 直连 200~618ms、syncing 态正常、账号卡片预填/保存/恢复 OK、**ALL 77 CHECKS PASSED**。

- **2026-09-13 第37轮：账号密码卡片精致化 + 额度占比条加粗**
  - 用户反馈：账号密码可放同一行、显示密码按钮丑、额度占比条太细。
  - ① 账号+密码合并单行（账号[输入] 密码[输入] [显示/隐藏]），节省纵向空间更紧凑。
  - ② 显示密码按钮改无边框文字按钮（「显示」↔「隐藏」随 echoMode 切换，hover/checked 变蓝），替代 👁 emoji。
  - ③ 额度占比条加粗：`sn_prog_h` 标准模式 6→10、大窗口 7→12，进度条更醒目。
  - 验证：账号密码同行、显示/隐藏文字切换正常、进度条高度 10px、**ALL 77 CHECKS PASSED**。

- **2026-09-13 第38轮：模型卡 token 九位数遮挡 + 悬浮框闪烁修复**
  - **① token 九位数被占比条挡住**：根因 `fmt_full` 完整千分位（9 位数 = 13 字符）在 `total_w=68px` 放不下溢出被进度条遮挡 → `total_w` 标准 68→100、大窗口 82→112。
  - **② 模型卡悬浮框闪烁**：根因与柱状图不同——ModelRow 只用 `enterEvent` 一次性显示固定位置(行中间顶部)的悬浮框，无 `mouseMoveEvent` 跟随，ToolTip 顶层窗口显示时抢焦点触发 enter/leave 抖动 → 闪烁。修复：① ModelRow 加 `mouseMoveEvent` 持续跟随鼠标（与柱状图一致）；② `_show_tip` 接受鼠标全局位置；③ ChartTip.show_tip 加去重（内容+位置未变跳过 resize/move，避免高频重复操作）。
  - 验证：show_tip 去重正常（同位置跳过/新位置刷新）、**ALL 77 CHECKS PASSED**。

- **2026-09-13 第39轮：商汤页视觉统一重构（小细条设计语言 + SNPromoCard 双列平衡布局）**
  - 用户交付重构代码并核查通过后替换：SNPoolCard 顶栏从胶囊徽标改「小细条 bar_indicator」统一设计语言，额度数值字号加大（lbl_pt 9.6/10.6、val_pt 10.6/11.8）更醒目；周/5h 双进度对称仪表保留。
  - **新增 SNPromoCard**：活动固定积分按「图2」双列居中平衡布局——左列总量余额大数字、中间分割线、右列最近到期日期（**黄色胶囊徽章**）+ 到期额度大数字；`set_data(total, expire)` 智能拆分日期与额度（兼容 `·` 分隔格式）。
  - SNSyncPanel / SNAccountCard 也统一 bar_indicator 小细条标题语言（蓝色 3.5px 圆角条）；SNQuotaPage 改用 SNPromoCard 组件。
  - **核查保留**：promo_bar 兼容别名、pools_layout、status_lbl/set_syncing/_sync_test、无「本地估算」文案、无 cite 残留、sn_prog_h 加粗(10/12)兼容。
  - 验证：冒烟（双池 bar_indicator/SNPromoCard 双列+黄色胶囊+日期额度拆分/promo_bar 别名/深色/空态）OK、**ALL 77 CHECKS PASSED**。备份 card_app.py.pre_v39.bak。

- **2026-09-13 第40轮：商汤小细条统一（BarIndicator 自绘）+ SNPromoCard 精简 + promo 改 emoji**
  - **用户反馈**：① 商汤页小细条与 WB/DSH 页不统一；② 赠送积分卡两个大数字太大、卡片太高，挤占上方额度卡高度。
  - **① 小细条统一**：WB/DSH 页 StatCard 自绘指示条为 16×3 圆角 1.5px；商汤页 bar_indicator 原为 14×3.5 圆角 1.75px → 统一 16×3 + 1.5px（SNPoolCard/SNPromoCard/SNSyncPanel/SNAccountCard 4 处）。
  - **② 精简 SNPromoCard**：大数字字号 18/21 → 13/15，标签 8.8/9.5 → 8.5/9.0，边距/间距压缩（margins 12→8、spacing 8→6、列内 3→1）。
  - **顺带压缩**：SNSyncPanel / SNAccountCard 边距 11→6、spacing 7→5、输入框 42→34/32，缓解纵向拥挤。
  - **高度验证（量化）**：内容总需求从 505px → 452px（workspace 480px），额度卡（SNPoolCard 139px）不再被压缩截断、内容完整。
  - **用户二次反馈**：QFrame+QSS 的 border-radius 对 3px 高元素渲染不可靠（实际无圆角、偏长），且活动积分卡不要小细条要 🎁 emoji。
  - **根治（本轮补强）**：新增 **BarIndicator 自绘组件**（QPainter drawRoundedRect(0,0,16,3,1.5,1.5)，与 WB/DSH StatCard 完全同款），SNPoolCard/SNSyncPanel/SNAccountCard 3 处改用（pool 跟随 accent、sync/account 蓝色）；SNPromoCard 去掉小细条改 🎁 emoji 标识。
  - **用户三次反馈**：额度卡小细条左边仍看不出圆角（像素级定位：BarIndicator 继承 QWidget 默认背景填充圆角外角部 → 圆角视觉消失）；SNSyncPanel/SNAccountCard 小细条颜色应区分。
  - **最终根治**：BarIndicator 高度 3→4、圆角 1.5→2.0，且 `setAttribute(Qt.WA_TranslucentBackground)` 让角部真正透明透出父背景（像素验证角部 alpha=0，圆角肉眼可见）；SNSyncPanel 保持品牌蓝 `#3b6fe0`、SNAccountCard 改紫色 `#8b5cd6` 区分。
  - 验证：BarIndicator 角部像素 (0,0,0,0) 透明、左/右侧实色、顶/底半透明过渡；颜色 sync=#3b6fe0 acct=#8b5cd6 不同；promo_icon=🎁；**ALL 77 CHECKS PASSED**。

- **2026-09-15 第41轮：商汤凭证获取失败修复 + 默认日期改今日**
  - **用户反馈**：时不时报「商汤凭证获取失败」，一般重启就好，但某次重启多次没好、放着过一会才自动好，影响体验。
  - **根因**：① `_sn_direct_fetch` 无重试、无系统代理绕过（瞬时网络/代理抖动一次失败即放弃）；② 程序**无周期自动刷新定时器**（refresh 只在启动/手动/切源触发），启动时失败后只能等重启或手动刷新，「每 5 分钟自动更新」只是文案无实锤。
  - **修复**：① `_sn_direct_fetch` 加**系统代理 10061 绕过直连重试** + **瞬时错误退避重试 3 次**（间隔递增）；② CardWindow 新增 `_auto_refresh_timer`（5 分钟周期刷新，失败自动重试）。
  - **顺带**：默认展示日期「全部」→「今日」（`self.range` + `btn_today.setChecked`）。
  - 验证：重试 mock（前2次瞬时失败+第3次成功）、周期定时器 300000ms、默认 range=today、**ALL 77 CHECKS PASSED**（注：当时 §8 桌宠素材 FAIL 为并行会话素材演进未同步测试的既有问题，本次单独修复）。

- **2026-09-15 第42轮：修复回归 §8 桌宠素材测试 FAIL（既有问题）**
  - **现象**：`test_switch_race.py` §8「v1 素材加载 (6 组)」FAIL——并行会话把素材体系升级到 v1~v4（PET_ANIMS 6组→8组：idle/sleep/wake/drag/click/sidle/pat/special），但测试断言仍停留在旧 6 组体系。
  - **实测各主题**：v1=5组(idle7/sleep2/drag6/click5/special9)、v2=5组(idle4/sleep2/drag3/click2/special2)、v3=7组(含 wake/sidle/pat)、v4=6组(含 wake/sidle)。
  - **修复**：重写 §8 适配新体系——① 素材断言改实际组数/帧数；② 状态机测试用新方法 `_pet_pat`(摸摸头)/`_pet_sidle`(特殊待机) 替代已删除的 `_pet_special`/`other` 小剧场；③ 补 v3 素材加载断言；④ 「静止为主」阈值 20%→40%（`_pet_idle_seq` 新实现 nonstatic 采样 max 31%，20% 过紧）。
  - 验证：**ALL 79 CHECKS PASSED**。

---

## 三、项目结构说明

> 目录整理于 2026-09-10；**2026-09-19 第 53 轮重排**：历史备份、调试中间产物、一次性脚本、
> 废弃审查页**统一移入 `_archive/` 按类型分档**（不入库，见第五节与 `_archive/README.md`）。
> 项目内只保留「跑起来必需 + 当前在维护」的东西。

```
token-stats/                  (F:\杂事\token-stats\)
├── card_app.py                # ★ 当前主源码（UI 层 + 商汤同步，PySide6，~5500 行）
├── scanner.py                 # ★ 数据引擎（jsonl 增量扫描聚合，纯标准库）
├── peer_store.py              # ★ 多机数据源（导出/导入/合并/命名）
├── test_switch_race.py        # ★ offscreen 回归测试（268 项断言；备份/恢复用户配置）
├── launcher.py                # 轻启动器源码（PyInstaller onefile 打包为 TokenStats.exe）
├── TokenStats.exe             # 编译产物（改 launcher.py 后必须重打包才生效）
├── sn_login.py                # 商汤登录
├── sn_autologin_fetch.py      # 商汤积分抓取
├── start_card.bat             # 启动器（ASCII，规范名）
├── 启动统计悬浮球.bat          # 启动器（中文别名，内容同 start_card.bat）
├── 启动统计悬浮球.vbs          # ★ 静默启动器（wscript 无窗口，日常主推）
├── enable_autostart.bat       # 注册开机自启（生成启动文件夹快捷方式）
├── 桌面图标指向exe.bat         # 生成桌面快捷方式
├── 商汤自动登录.bat            # 商汤登录入口
├── 重新打包exe.bat             # 重新打包 exe
├── 协作进度.md                 # ★ 本文件（主协作文档）
├── README.md                  # 项目说明（面向使用者：简介/启动/同步配置/贡献指南）
├── .gitignore                 # 版本库排除规则
├── assets/                    # 现役素材
│   ├── pet/                   # ★ 桌宠 v1 经典素材（41 帧，入库）
│   ├── pet_v2/                # ★ 桌宠 v2 新版素材（高清，13 帧）
│   ├── pet_v3r/               # ★ 桌宠 v3 最新素材（与原版合并，46 帧）
│   ├── pet_v4/                # ★ 桌宠 v4 deepseek娘V4Pro（12 帧）
│   ├── pet_alt/               # 退役帧存档（★ 测试依赖 §8，勿动）
│   ├── pet_v3_add/            # 补充帧（★ 测试依赖 §8，勿动）
│   ├── *_qc.json / *_windows.json  # 各版本质检与窗口配置
│   └── app_icon.{ico,png}
├── docs/                      # 文档与预览
│   ├── 素材饱和度优化.md
│   ├── V3补充素材清单.md
│   └── assets/previews/       # 界面预览图（offscreen 渲染素材，入库）
├── tools/                     # 现役脚本
│   ├── syntax_check.py        #   全量语法检查
│   ├── restart_app.py         #   重启卡片应用
│   ├── build_exe.py           #   打包主程序 exe
│   ├── build_launcher.py      #   打包轻启动器
│   ├── make_app_icon.py       #   生成应用图标
│   └── metrics_regression.py  #   指标回归
└── _archive/                  # ★ 归档区（不入库，见 _archive/README.md）
    ├── README.md              #   归档清单与取回方法
    ├── assets-debug/          #   根级调试截图（22 项 / 16.5 MB）
    ├── assets-pet-legacy/     #   桌宠历史版本（12 项 / 53.0 MB）
    ├── docs-reviews/          #   历史审查页（15 项 / 58.1 MB）
    ├── root-legacy/           #   根目录历史物（3 项 / 9.1 MB；含原 legacy/）
    └── tools-once/            #   一次性诊断脚本（46 项 / 259.9 KB）
```

**层级关系**：根目录为唯一源码所在。`_archive/` 集中存放历史物（磁盘保留、不进版本库）。
当前运行依赖根目录的核心文件：`card_app.py`、`scanner.py`、`peer_store.py`、启动器。

**归档纪律**（第 53 轮建立）：新产生的历史物（调试截图、素材旧版、一次性脚本、废弃审查页）
**直接放 `_archive/` 对应分类**，不要堆回项目目录 —— `_archive/` 已被 `.gitignore` 一条规则排除。

**外部数据与运行文件**（不在仓库内）：
- 数据来源：`~/.workbuddy/projects/**/*.jsonl`（WB 会话）、`~/.dsh/.../cost-meter/ledger.json`（DSH 账本）
- 持久化目录：`~/.workbuddy/plugins/data/token-usage-stats/`
  - `cache.json`（扫描增量游标）、`stats.json`（聚合结果）
  - `sn_autosync.json`（自动同步配置+字段路径；手动同步已移除，不再有 `sn_points_sync.json`）
  - `sn_events_cache.json`（商汤事件缓存）、`settings.json`（主题/数据源）、`ball_pos.json`（球位置）
  - `peers/<机器名>.json`（别机数据源快照）

---

## 四、构建 / 运行 / 测试说明

1. **运行源码（日常使用）**：双击 `启动统计悬浮球.vbs`（静默）；或
   `%USERPROFILE%\.workbuddy\binaries\python\envs\pyside6\Scripts\pythonw.exe card_app.py`
2. **单独调试扫描引擎**：
   `%USERPROFILE%\.workbuddy\binaries\python\envs\pyside6\Scripts\python.exe scanner.py --force`
3. **回归测试（改动后必跑）**：
   ```
   set QT_QPA_PLATFORM=offscreen
   %USERPROFILE%\.workbuddy\binaries\python\envs\pyside6\Scripts\python.exe test_switch_race.py
   ```
   268 项断言需全绿（测试会自动备份/恢复用户真实配置，商汤/settings/peers 全隔离到临时目录，可放心跑）。
4. **全项目语法检查**（**不要用 `python -m py_compile`**，见 §1.3 铁律 3）：
   `%USERPROFILE%\.workbuddy\binaries\python\envs\pyside6\Scripts\python.exe tools\syntax_check.py`
5. **提交前三步**：语法检查 → 全量回归 → 更新本文档。

---

## 五、版本管理与提交规范（Git · GitHub）

> 仓库托管在 GitHub：**https://github.com/Suqianqiana/token-stats**，默认分支 **`main`**（第 56 轮接入）。
> 两位协作者通过 `clone` / `pull` / `push` 协同；日常节奏「开工 `git pull`、收工 `git push`」。
> 协作硬规矩（禁止 rebase/amend 已推送提交、`_archive/` 不入库、运行期数据不在仓库）见 `README.md` §7。

### 5.1 .gitignore 规则
```
# 归档区：历史物集中存放，磁盘保留、不进版本库
/_archive/

# 字节码与缓存
__pycache__/
*.pyc

# 运行日志
*.log

# 调试与临时产物
/_*.png
/assets/_*.png
/_*.txt
*.bak
*.bak.*

# 本地快捷方式 / 误生成的重复副本
*.lnk
/ (*).*

# 打包中间产物
/build/
/dist/
*.spec
!TokenStats.spec
```
> **2026-09-19 第 53 轮收敛**：原先逐条列的 `legacy/`、`/assets/pet_v2_bak_*/`、
> `/docs/*_review.html` 等排除规则**全部失效**（那些路径已不存在，历史物已集中到 `_archive/`），
> 收敛为一条 **`/_archive/`**。归档纪律：新历史物直接放 `_archive/` 对应分类，无需再改本文件。
> 预览图（`docs/assets/previews/`）作为文档素材**入库**。

### 5.2 提交约定（每轮改进 / 修复都必须执行）
1. 每完成一轮，必须提交一次，信息格式：`第N轮：<一句话说明本次改动>`。
2. 提交前确认工作区无重要未跟踪文件；`legacy/`、`__pycache__`、日志已由 `.gitignore` 排除。
3. 主源码只改 `card_app.py`、`scanner.py`；改完先 `tools/syntax_check.py` 语法检查 + offscreen 回归全绿，再提交。
4. 每轮改动**先更新本《协作进度》**（§二 演进历程 + §八 协作者备注），`git commit` 紧随其后。
5. 回滚：`git log --oneline` 查哈希 → `git checkout <哈希>` 查看旧版；`git reset --hard <哈希>` 彻底回退（慎用）。
6. **版本号策略**：以「第N轮」提交轮次为主线；`card_app.py` 头部注释「Token 审计卡片 V7」为窗口形态标识，架构级演进时才升版（升版轮次需同步：① 源码头部注释；② 本文档 §一/§二；③ 全量回归通过——三处一致方可提交）。

### 5.3 常用命令
- 提交：定点添加后提交，如
  `git add 协作进度.md card_app.py test_switch_race.py` 再 `git commit -m "第N轮：说明"`
  （**勿用 `add -A`**：养成只提交相关文件的习惯）
- 历史：`git log --oneline`；状态：`git status`
- 推送：`git push` 推到 `origin/main`（首次授权见 `README.md` §7.1；凭据存 Windows 凭据管理器后免输）。

---

## 六、已完成 / 已知修复

- [x] 增量扫描聚合引擎（184 文件 6.2s 全量 / <0.5s 增量；后引入事件缓存商汤侧 14.4s→0.159s）
- [x] PySide6 无边框卡片窗口 + 悬浮球（置顶/拖动/位置记忆/最小化隐藏不退出）
- [x] 刷新卡死根治（工作线程直调 QTimer → RefreshBridge 信号槽）
- [x] 液态玻璃主题定论（纯 Qt 分层半透明 + 高光描边；系统级模糊 API 全部封死勿再试）
- [x] 深浅色模式 + 暗色灰字三轮提亮（TEXT2/TEXT3 对比度达标）
- [x] 启动器演进定论（bat 必闪窗 → VBS 静默；自启 vbs 优先注册表兜底）
- [x] 热力图真日历对齐 + 竖排月历
- [x] 窗口宽度锁死 664（QLabel 长文本撑爆 minimumWidth 真因 + Ignored/WordWrap 解法）
- [x] DSH 数据源（ledger.json 唯一权威源，规避 usage-cache 翻倍 bug；花费/请求数/口径统一）
- [x] 商汤积分页（官网风三卡：通用池/Flash-Lite 池/活动固定积分；官方口径 60000/5h、600000/周）
- [x] 商汤 cURL 自动同步（bash/cmd 解析、指纹自学习、真实接口字符串数字、精确透传、官方重置时间、活动积分上卡）
- [x] 零指纹结构探测（贴 cURL 即配，数字框全空）
- [x] 切页跨源渲染竞争根治（源快照 + pending 补刷）
- [x] 5h 窗口翻滚回退估算 / 手动⟳ 绕过 TTL 频控 / 401 回退本地估算
- [x] 内嵌同步面板 SNSyncPanel（取代二级对话框）+ 单列大卡 + 防右侧截断布局
- [x] 窗口默认位置统一（place_right 固定基准 880，各页顶部对齐）
- [x] WinError 10061 自动绕过系统代理直连重试
- [x] 保存流程全面异步化（后台线程 + Signal，修 UI 挂起崩溃；worker 异常兜底）
- [x] 回归测试体系（offscreen；测试自动备份/恢复用户真实同步配置）
- [x] 项目目录整理（legacy/ 弃用归档、docs/ 文档素材）+ 本地 Git 初始化
- [x] 桌宠形态（DeepSeek 娘）：单图素材拆帧 46 张入库 + BallWindow 帧动画状态机（待机/入睡/拖拽/点击互动/小剧场/摸摸头）+ 双形态切换（offscreen 回归 67 项）
- [x] 全程序视觉基座统一：按钮四档工厂 + 自绘弹窗三工厂 + `NavButton` 纯自绘（第51轮）
- [x] **自绘弹窗"看不见"根治**：基类 `QFrame`→真 `QDialog` + 原生 `exec()`，尺寸由布局决定（第52轮）
- [x] **同步进行中切页卡住根治**：抽 `_apply_page()`，视图切换与数据渲染解耦（第52轮）
- [x] **暗色下统计框错色根治**：`MachineRow` 不再嵌套 `GlassPodFrame`（双层玻璃叠加），改普通 `QWidget` 单层底（第52轮）
- [x] **局部 QSS 顶掉全局滚动条样式**：`style_scroll_area()` 打包规则 + `apply_theme()` 重套（第52轮）
- [x] 多机页由 13:9 双栏改**单列纵向三段**；导入按钮统一为「导入/更新别机数据」并区分新增/覆盖反馈（第52轮）
- [x] **弹窗输入框暗色下仍是纯白根治**：`add_field()` 补 `apply_theme()`（`__init__` 里 `self._field` 尚为 `None`，样式分支从未执行）（第53轮）
- [x] **追加输入框后弹窗不长高**：`_fit()` 逐层 `invalidate()`（内层 `_wrap_lay` 先于外层）+ `setMinimumHeight(0)` 解旧锁（第53轮）
- [x] **改名改就地内联编辑**：`MachineBox` 内嵌常驻 `QLineEdit` + `name_edited` 信号；Enter/失焦/保存提交，Esc/取消放弃（第53轮）
- [x] **按机统计"刷的一下出来"根治**：`pending` 骨架占位行（同高 70px）+「正在汇总…」文案（第53轮）
- [x] **列表重绘资源泄漏根治**：`_clear_layout()` 取全部 item（含 `addStretch` spacer）+ `setParent(None)`（第53轮）
- [x] **项目文件夹整理**：98 项 / 137.0 MB 历史物移入 `_archive/` 五分类；`.gitignore` 收敛为一条 `/_archive/`；`git rm --cached` 51 项（第53轮）

## 七、待验证 / TODO

- [ ] 商汤 JWT 约 3 小时过期：过期后回退本地估算并提示——待用户长期使用中验证回退体验
- [ ] 网络异常场景矩阵验证（代理开着/关着/直连被防火墙拦）——10061 已覆盖，超时/重置类未覆盖
- [ ] 桌宠实机观感验证（帧动画节奏/入睡时机/双击开面板在实机的手感；offscreen 仅验证逻辑）
- [ ] 多机页第52轮改版实机验收（同步中切页是否即时、改名/删除/导出/导入四处弹窗是否可见、暗色下框框底色、单列三段观感）
- [ ] 第53轮实机验收（弹窗输入框暗色表现、就地改名三种提交方式、进入多机页时统计条不再闪）
- [ ] （可选）special「10 读书」帧因与大立绘粘连未拆出（09 帧已含读书画面），如需可手工精修
- [ ] （可选）关闭=hide 的多实例治理（旧实例残留导致「改了没生效」误判，V17 教训）
- [ ] （可选）启动器路径硬编码本机 venv——跨设备分发需改为相对探测
- [ ] （可选）offscreen 测试环境中文方框（仅影响测试截图，实机正常；可考虑测试机装字体）
- [ ] （可选）`_archive/` 稳定一段时间后整体打包外移或删除（当前保留以留回退路径）

## 八、协作者备注区

> 每次修改后请在此留下简短记录，方便接手助手同步。先读本文档再动手，避免重复劳动或覆盖他人改动。历史轮次的详细改动见 §二 演进历程。

### 2026-08-25 ~ 2026-09-08 浅浅猫 × WorkBuddy（V1~V28 补录）
- 完成网页悬浮球 → PySide6 卡片窗口 → 玻璃主题 → DSH 源 → 商汤积分页 → cURL 自动同步的完整演进，细节见 §二 对应日期条目；期间沉淀两条铁律：玻璃只能纯 Qt 分层自绘、UI 线程永不碰网络/重 IO。

### 2026-09-10 浅浅猫 × WorkBuddy（UI 重构 + 网络修复 + 异步化）
- 商汤页单列大卡 + 内嵌 SNSyncPanel；place_right 顶部统一；活动卡/池卡防截断。
- WinError 10061：系统代理未运行 → `_http_json` 自动直连重试。
- 保存卡死崩溃：`_on_save` 网络请求移入后台线程（`save_finished` 信号回 UI），`_on_sn_sync_saved` 改走 refresh 后台通道；回归 54 项全绿。

### 2026-09-10 浅浅猫 × WorkBuddy（协作规范升级）
- 按 WBCacheCleaner《写作进度》格式重建本主文档（原 docs/ARCHITECTURE.md 内容已并入并删除）。
- 初始化本地 Git 仓库：初始提交（现状入库）+ 本轮文档提交；`.gitignore` 排除 legacy/ 与缓存；约定「第N轮：说明」提交格式，仅本地不上云。
- 文档由用户定名《协作进度.md》（重命名自《写作进度.md》），内容不变。

### 2026-09-10 浅浅猫 × WorkBuddy（第2轮：桌宠形态）
- 拆帧管线：腐蚀去框线 → 连通域 + 列投影分帧 → flood-fill 去白 → 白碎屑清除 → 内容 bbox；46 帧入库 `assets/pet/`（special 10 读书帧与大立绘粘连弃用）。
- BallWindow 双形态 + 帧动画状态机（idle/sleep/drag/click/other/special），桌宠模式双击开面板；素材缺失自动回退悬浮球。
- 回归扩至 67 项全绿；目录清理：顺带移除工具误生成的 `%SystemDrive%/` 缓存目录。

### 2026-09-10 浅浅猫 × WorkBuddy（第3轮：桌宠修补）
- 抠图修复：实心主体掩码法（fill_holes 保护 + 8 连通背景判定 + 封闭腔分级），衣/脸/发饰白色完整保留、背景白与发丝碎片清干净。
- 动画节奏：待机改随机段式（静止 4~10s + 低频眨眼/小动作），小剧场 90~240s 一次 3 帧，入睡长静止慢换姿。
- 回归 67 项全绿。

### 2026-09-10 浅浅猫 × WorkBuddy（第4轮：背景残留根治 + 单实例治理）
- 背景白真因：环抱背景腔（数千 px）被尺寸启发式误留 → 改按**腔内平均饱和度**分级（<5.5=背景透明，≥5.5=衣饰白保留）。
- 「节奏没变化」真因：旧实例驻留（✕=隐藏不退出）→ 新增 `_kill_stale_instance()` PID 守护，双击启动器自动杀旧进程加载新代码。
- 节奏再拉长（静止 6~17s / 小剧场 150~420s）；回归 67 项全绿。

### 2026-09-10 浅浅猫 × WorkBuddy（第5轮：节奏失效真因修复）
- 真元凶：`paintEvent` 取帧误用 `_si % len(frames)`（序列索引当帧号），`_seq` 节奏逻辑从未生效——用户三次反馈"还是旧循环"均由此起。
- 修复取帧 + 新增"实际展示帧"回归断言（68 项全绿）+ 右键菜单显示 APP_BUILD 版本号。
- 已替用户启动新实例（PID 守护确认存活）。

### 2026-09-10 浅浅猫 × WorkBuddy（第6轮：发饰/枕头修复 + 拖拽随机姿势）
- 封闭白腔全保留 + sleep 闭运算掩码（被子/枕头完整）+ 边界连通按内切半径分级；click 行 y1 收紧去数字；drag 改随机保持一个被拎姿势；sleep_03 移除（剩 5 帧）；click_01 整块保留 + 白色孤立块清理。
- 回归 68 项全绿。

### 2026-09-10 浅浅猫 × WorkBuddy（第7轮：click_01 弃用）
- 用户决定放弃 click_01（贴边+棋盘格装饰，问题最多），click 组改用 5 帧（惊讶/开心/生气/头顶星星/爱心）。
- 最终帧数 idle7 / sleep5 / drag6 / click5 / other12 / special9 = 44 帧；回归 68 项全绿；已重启实例生效（PID 守护自动换新）。

### 2026-09-10 浅浅猫 × WorkBuddy（第8轮：背景白真正根治——发现棋盘格底纹）
- 用户反馈"背景又回来了"（v14 封闭腔全保留的副作用）。深挖发现：**原素材背景是棋盘格纹理**（透明底的 JPG 化产物），不是纯白——历次残留皆源于此。
- v16 算法：M 外全透明（清背景+棋盘格）；M 内封闭腔双判定（通透性 >30% 删细围栏腔 / ≥1000px 且内切 ≥20 删环抱开阔腔 / 其余=衣饰白保留）。46 帧重生成（生效 44），逐组目检全部达标。
- 回归 68 项全绿；已重启实例（PID 5356）。

### 2026-09-10 浅浅猫 × WorkBuddy（第9轮：接手重做抠图 + 量化质检）
- 承认前三轮反复（棋盘格 ↔ 内部白）与评估缺陷（目视漏检 22 帧破洞、未征询用户）。
- v20 最终算法：中性浅色泛洪清背景（内部白由描边天然保护）+ 封闭孔 <600px 填回 / ≥600px 透明 + 边缘收白。
- 按用户决定：sleep 仅留 01/02；special 发饰重处理修复；41 帧质检 41/41 通过；出具 `docs/pet_qa_report.html`。

### 2026-09-10 浅浅猫 × WorkBuddy（本轮：商汤页瘦身 + 切页高度修复）
- 商汤页**手动同步全链路移除**（`sn_points_sync.json`/`load_sn_sync` 等 + manual 回退分支 + 保存写回退 + UI 残留分支），自动同步为唯一积分来源；同步面板「清除配置」只清自动同步。
- **切页高度 bug 根治（两轮迭代）**：首轮用 `SetNoConstraint` 致实机切页卡住被推翻 → 改 `render()` 同步收口仍致日期切换卡死（resize-布局事件风暴饿死 paint，日志全链路执行完但 UI 冻结）→ **终版收口延后一拍**（`QTimer.singleShot(0, self._settle_height)`）+ **商汤页点日期自动切回 WB**。验证 INIT 904 / SN 514 / WB 880 / 回归 64 项全绿。
- 悬浮球右键新增 **🔄 一键重启**（pythonw 静默拉起新实例 + PID 兜底杀旧）。
- 回归断言 68 → 64 项全绿（删手动同步两组用例，401 改断本地估算）。

### 2026-09-11 浅浅猫 × WorkBuddy（第13轮 + 第14轮）
- 第13轮：商汤页清理「本地估算」残留文案 5 处（状态行/清除提示/页脚/tooltip/subtitle），剩余估算字样仅在注释。
- 第14轮：**商汤页切页闪旧根治**——根因=_sn_cache 在停留商汤页刷新完成时不更新（只在切走时更新），切回用过期缓存先渲染空态再刷新变新页；修复= _on_scan_done 的 if src==self.source 分支加 if src=="sn": self._sn_cache = s。验证：往返切页不再闪空态，回归 64 项全绿。待用户一键重启实测确认。

### 2026-09-13 浅浅猫 × WorkBuddy（第25轮：横版大改版 V8.2）
- 采用用户交付的横版重构版（990×610 固定尺寸 + 左侧导航栏 + 桌宠智能吸附展开），替换原竖版自适应布局；第15~24轮桌宠 v2 素材精修已并入。
- 修复交付版 6 项严重缺陷：① [cite: 1] 残留×3（商汤自动同步后点页必崩）② 补 _find_value_paths ③ promo 金额精度/兜底 ④ 第13轮文案回退 ⑤ 桌宠 click/special seq len ⑥ _sync_test 钩子。
- 回归适配新版接口后 **77 项全绿**；备份旧版 card_app.v24.worktree.20260913.bak.py。待用户一键重启验证横版观感与桌宠吸附。

### 2026-09-13 浅浅猫 × WorkBuddy（第26轮：商汤官网风格组件现代化重构）
- 采用用户交付的商汤页精细重构（渐变进度条/双栏池卡+彩标徽章/胶囊状态徽标/优雅 promo 横幅）。
- 审核修复 4 项：①「本地估算」文案二次回退 → 恢复第13轮定稿（用户认可底线不可回退）② _sync_test 钩子补回 ③ 错误文案补「商汤」二字 ④ status_pill 兼容别名 status_lbl。
- 验证 ALL 77 CHECKS PASSED + 三态状态文本 + 冒烟渲染通过。待用户重启看新商汤页观感。

### 2026-09-13 浅浅猫 × WorkBuddy（第27轮：商汤页纵向多层级卡片）
- 用户反馈双栏挤压/重叠 → 采纳新布局：SNPoolCard 纵向三层（徽标+池名+适用模型 / 周额度+周重置 / 5h通栏进度条+重置独占行），promo 横幅 36。
- 择优保留上版：渐变进度条 + 胶囊状态徽标（用户明确要）。修复 SNSyncPanel 漏初始化 _status_msg（实例化即崩）。
- ALL 77 CHECKS PASSED；备份 card_app.py.pre_v27.bak。待用户重启看新纵向布局观感。

### 2026-09-13 浅浅猫 × WorkBuddy（第28轮：商汤对称仪表 + promo 双行 + 图表缩放平移）
- 核查发现交付文件实为旧版（无三项改进+带 cite 致命残留），故未替换，改为在当前最新版直接实现三项需求。
- SNPoolCard 周/5h 完全对称双仪表（标签+数值占比+圆角渐变进度条+专属重置时间）；promo 双行卡片（标题+规则 / 总额+到期）；柱状图滚轮锚点缩放（Y 轴随视口峰值自适应）+ 拖拽平移。
- 修复缩放方向写反、视口索引边界；ALL 77 CHECKS PASSED + 冒烟（zoom/pan True）。待用户重启验证手感。

### 2026-09-13 浅浅猫 × WorkBuddy（第29轮：iOS 27 液态玻璃效果）
- 采用新版（2987 行）：LiquidGlassFrame 主底板（微折射渐变+光随鼠动光斑+菲涅尔边缘）+ GlassPodFrame 全子卡玻璃化，纯 Qt 自绘无系统 API（符合铁律）。
- 核查仅补 2 处：_find_value_paths 补回、promo_card 加 promo_bar 兼容别名；其余（对称仪表/promo双行/缩放平移/_sync_test/胶囊/无估算文案）全保留。
- 验证：玻璃渲染/光斑随鼠动/移出清除 + 全功能冒烟 OK，ALL 77 CHECKS PASSED。待用户重启看玻璃观感。

### 2026-09-13 浅浅猫 × WorkBuddy（第30轮：桌宠重启显示大球 bug 修复）
- 桌宠模式重启后显示大悬浮球：根因=__init__ 桌宠分支漏设 self.pet=True（仅 _pet_apply(True) 改尺寸），paintEvent/动画判定失效。
- 修复：分支内补 self.pet = True。验证双向切换 + 77 项全绿。待用户桌宠模式重启确认。

### 2026-09-13 浅浅猫 × WorkBuddy（第31轮：虹彩液态玻璃 v8.6）
- 采用新版（3213 行）：虹彩渐变 create_iridescent_gradient + 窗口尺寸模式（default 990/large 1180）+ 玻璃透明度三档（crystal/balanced/frosted）+ 组件 apply_size 缩放。
- 核查修复 2 项：[cite: 1] 残留回退（商汤同步计算必崩）删除、_find_value_paths 补回；桌宠修复/promo_bar 别名/_sync_test/胶囊/无估算文案全保留。
- 验证：尺寸切换 990↔1180、透明度三档、虹彩渲染、桌宠修复保留，ALL 77 CHECKS PASSED。待用户重启体验新玻璃观感。

### 2026-09-13 浅浅猫 × WorkBuddy（第32轮：v8.7 晶透水滴玻璃预设）
- 采用新版（3237 行，v8.7-crystal-glass）：玻璃通透度三预设（crystal/balanced/frosted）入主题 + 右键菜单「🔮 玻璃通透度」「📐 窗口尺寸」（990×610/1180×730）+ 组件尺寸经 curr_metric() 集中管理。
- 核查修复 2 项：cite 残留×2 删除、_find_value_paths 补回；桌宠修复/promo_bar/_sync_test/胶囊/无估算文案保留。
- 验证：预设/尺寸/透明度/桌宠冒烟 OK，ALL 77 CHECKS PASSED。待用户重启体验晶透水滴观感。

### 2026-09-13 浅浅猫 × WorkBuddy（第33轮：启动/重启提速 + 桌宠流畅性）
- 剖析定位耗时：import 260ms（PySide6 固有）+ BallWindow 41ms（素材全量加载）。
- 优化：① 素材懒加载（非桌宠启动 BallWindow 41→1.4ms，-97%，开启时一次加载+缓存）② 重启 300→150ms ③ 桌宠静止帧跳过重绘（-96%，120 tick 120→5 次 update）。
- 验证 ALL 77 CHECKS PASSED。待用户重启感受启动/切换速度。

### 2026-09-13 浅浅猫 × WorkBuddy（第34轮：桌宠开关消失修复）
- 懒加载副作用：菜单显示条件用 pet_frames 是否已加载判断，非桌宠启动时消失。
- 修复：改判素材目录存在（os.path.isdir），点击开启时才懒加载。验证 77 项全绿。待用户右键确认桌宠开关回归。

### 2026-09-13 浅浅猫 × WorkBuddy（第35轮：商汤积分真自动凭证刷新）
- 采用 Playwright 持久登录态（复用系统 Edge），登录态自动抓取 pool-usage 积分接口 + 凭证过期自动重登，彻底告别 3h 手动抓包。
- 新增 sn_login.py（全自动登录）/sn_autologin_fetch.py/sn_autologin_fetch 启动器；账号存本机 sn_account.json 不进 git。
- card_app 集成 sn_playwright_fetch + SN_USE_PLAYWRIGHT 开关；测试关闭开关走 cURL mock。ALL 77 CHECKS PASSED + 端到端 load_sn_stats 真自动验证通过。
- 待用户重启后观察：商汤页刷新应不再需要手动抓包，凭证过期自动续。

### 2026-09-13 浅浅猫 × WorkBuddy（第36轮：提速28倍+进度反馈+账号卡片）
- 速度：access_token 从 localStorage 提取，urllib 直连 pool-usage，同步 10~28秒→200~600ms；token 缓存 sn_token.json，401 回退 Playwright 重登。
- 进度：SNSyncPanel 加 set_syncing 橙色进度态；账号密码卡片 SNAccountCard 挂商汤页，持久化 sn_account.json。
- ALL 77 CHECKS PASSED。

### 2026-09-13 浅浅猫 × WorkBuddy（第37轮：账号卡片精致化+进度条加粗）
- 账号密码合并单行；显示密码按钮改文字「显示/隐藏」无边框；额度占比条 sn_prog_h 6→10/7→12。
- ALL 77 CHECKS PASSED。待用户重启看观感。

### 2026-09-13 浅浅猫 × WorkBuddy（第38轮：模型卡token遮挡+悬浮框闪烁修复）
- token 九位数被占比条挡住：total_w 68→100/82→112 加宽。
- 模型卡悬浮框闪烁：ModelRow 加 mouseMoveEvent 跟随鼠标 + show_tip 去重，根除 enter/leave 抖动。
- 版本号更新 v8.7-crystal-glass → v8.8-auto-sync（本版核心特性=自动同步）。
- ALL 77 CHECKS PASSED。待用户重启验证。

### 2026-09-13 浅浅猫 × WorkBuddy（第39轮：商汤页视觉统一重构）
- SNPoolCard 胶囊徽标→小细条 bar_indicator，额度字号加大；新增 SNPromoCard 双列平衡布局（总量余额/分割线/到期黄色胶囊+额度）。
- SNSyncPanel/SNAccountCard 统一小细条语言；SNQuotaPage 改组件化。核查保留 promo_bar/status_lbl/_sync_test/无估算文案；ALL 77 CHECKS PASSED。

### 2026-09-13 浅浅猫 × WorkBuddy（第40轮：小细条统一+SNPromoCard精简）
- 小细条根治：QFrame+QSS 圆角不可靠 → 新增 BarIndicator 自绘组件（QPainter 16×3 圆角1.5 与 WB/DSH StatCard 同款），SNPoolCard/SNSyncPanel/SNAccountCard 改用；SNPromoCard 去小细条改 🎁 emoji。
- SNPromoCard 大数字 18/21→13/15、边距压缩；SNSyncPanel/SNAccountCard 边距压缩、输入框 42→34/32；内容总需求 505→452px，额度卡不再被截断。
- ALL 77 CHECKS PASSED。待用户重启看统一后的观感。
- 小细条补强：BarIndicator 高度 3→4、圆角 1.5→2.0 + WA_TranslucentBackground（角部透明圆角可见）；SNSyncPanel 蓝 #3b6fe0 / SNAccountCard 紫 #8b5cd6 颜色区分。ALL 77 CHECKS PASSED。

### 2026-09-15 浅浅猫 × WorkBuddy（第41轮：商汤凭证失败修复+默认日期今日）
- 根因：_sn_direct_fetch 无重试/无代理绕过 + 程序无周期刷新定时器。
- 修复：加系统代理10061绕过+退避重试3次；新增5分钟周期自动刷新定时器；默认日期改今日。ALL 77 CHECKS PASSED。

### 2026-09-15 浅浅猫 × WorkBuddy（第42轮：修复回归§8桌宠素材测试FAIL）
- 并行会话素材升级 v1-v4(PET_ANIMS 8组)但测试停留旧6组断言。重写§8适配：素材断言改实际组数、状态机用_pet_pat/_pet_sidle新方法、补v3断言、静止阈值20%→40%。ALL 79 CHECKS PASSED。

### 2026-09-16 浅浅猫 × WorkBuddy（第44轮：回退效能洞察页+修复刷新卡死）
- 第43轮效能洞察页验收不及预期 → git revert 回退到第42轮（保留历史可恢复）。
- 刷新卡死根因：work线程卡死(Playwright launch无超时)时_scanning永远True按钮永久禁用。修复：看门狗定时器(超60s强制恢复)+launch加timeout=25000。ALL 79 CHECKS PASSED。

### 2026-09-16 浅浅猫 × WorkBuddy（第45轮：商汤凭据获取链路优化——失败率 / 速度 / 按钮状态）
> 需求：优化商汤凭据获取链路（先摸清实现与近期改动再动手）；现象是①时不时获取失败 ②获取凭据偏慢 ③更新失败时"更新按钮不刷新"。同时把 `tools/restart_app.py` 留下的孤儿实例清理干净。

**一、先摸清现状（本轮实测基线）**
- 链路：`sn_autosync_fetch` → 真自动 `sn_playwright_fetch`：①`_sn_load_token` 读缓存 token → `_sn_direct_fetch` urllib 直连 `pool-usage`（快路径）②失败/401 → `_sn_playwright_login_and_fetch` 拉无头 Edge 走控制台，必要时自动重登（慢路径）③再失败才回退 cURL 配置。
- 实测基线：**快路径 0.71s ✓ / 慢路径 13.01s**。token 是标准 JWT(RS256)，载荷自带 iat/exp，**寿命 10800s(3h)**（实测 07:28:52→10:28:52）。

**二、三个现象的根因（都拿到了证据）**
1. **时不时失败**（4 个独立原因叠加）
   - ①**cURL 回退被测试残留"投毒"**：`test_switch_race.py` 曾把 mock 配置写进**真实数据目录**，且结尾的恢复被 `if _user_autosync:` 挡住（前置 `clear_sn_autosync()` 已把它清成 `{}` → `load_sn_autosync()` 返回 None → 备份为空 → 恢复被跳过）。结果真机上常驻一份 `https://api.example.com/...` 的假凭据：只要真自动那次失败，就回退到这台不存在的域名，报一个与真实原因无关的 DNS 错误 → 看起来"同步坏了"。**已在磁盘上找到这份残留并归档。**
   - ②**Edge 路径写死**：`_sn_playwright_ready()` 只认 `C:\Program Files (x86)\...\msedge.exe`。Edge 装到 x64 目录/换盘/重装 → 判定为"不可用" → **整条真自动链路静默失效**并退化到 cURL。
   - ③**固定 sleep 5s 太短**：控制台 SPA 加载慢时 5s 内没抓到 `pool-usage` 响应 → 直接判「未捕获到积分数据」。
   - ④**登录态文件缺失即判死**：`os.path.exists(SN_LOGIN_STATE)` 不满足就跳过真自动（首次运行直接失败）。
   - 附带：`browser.close()` 散落在多处提前 `return` 之外，异常路径**会残留无头 msedge**，累积后拖慢/卡死后续抓取。
2. **慢**：慢路径 13.01s 里**有整整 5s 是固定的 `wait_for_timeout(5000)`**，外加 `goto` 用默认 `load`（等 SPA 全部子资源）。且**只有撞上 401 才续签**——token 一到期，用户正好点刷新就要等十几秒。另外 `_sn_direct_fetch` 超时 15s × 最多 3 次 = 最坏 45s+，会把失败场景拖到看门狗（60s）边缘。
3. **更新失败时按钮/状态不刷新**：
   - 失败/异常路径只改副标题，**同步面板会永久停在「⟳ 正在自动获取凭证并同步…」**——看起来就是"一直转圈、点了没反应"。
   - `refresh()` 在扫描进行中被打断时只记 `_pending_refresh`，**把 `force=True` 丢了**；补刷新退化成 `force=False` 会命中 300s TTL 缓存 → 数据看着"没更新"。
   - 看门狗 60s 超时后**在途线程还在跑**，它迟到的结果会覆盖新状态（状态串台），且可能叠出第二个扫描线程。
   - 按钮被 disable 时**视觉上看不出来**（面板两个按钮无 `:disabled` 样式）。

**三、本轮改动**
- **速度**：`wait_for_timeout(5000)` → **事件驱动等待**（每 150ms 轮询额度响应，一到就走，最多等 25s）；`goto` 改 `wait_until="commit"`；`SN_HTTP_TIMEOUT` 15→8s、重试 3→2 次（最坏 45s→约 18s）；新增**提前续期**：token 剩余 <15min（`SN_TOKEN_REFRESH_MARGIN`）时周期刷新顺手换证，用户几乎不会撞上慢路径。
- **失败率**：新增 `_sn_edge_path()`（固定候选 → 注册表 App Paths → `shutil.which` 兜底，带缓存）；去掉"必须先有登录态文件"的硬门槛（缺文件就用账号密码直接登录一次，首次运行也全自动）；非登录页没抓到 → `reload` 一次给二次机会；全流程 `try/finally` 保证关浏览器（不再留无头残留）；`_sn_direct_fetch` 401/403 立即返回不再无意义重试。
- **凭据域名校验 + 自愈**：`load_sn_autosync`/`save_sn_autosync` 只接受 `*.sensenova.cn`；非商汤域名**自动归档**为 `sn_autosync.invalid.json` 并记日志，从此不会再报误导性的 DNS 错误。
- **本地 JWT 过期预判**：`_sn_token_exp` 解析载荷 exp（带缓存），剩余寿命 <120s 时**跳过注定 401 的往返**直接走浏览器续期。
- **按钮/状态**：新增 `set_failed()`——失败/超时/异常一律把面板拉出 syncing 态；失败文案带出「上次成功 HH:MM」（`last_ok_time`）；`refresh(force, proactive)` 合并请求时**保留 force**；新增**扫描代次 `_scan_gen`**，迟到结果直接丢弃；按钮忙碌时改成「⟳ 同步中…」+ 补 `:disabled` 样式；看门狗超时也同步复位面板。
- **可观测性**：新增 `sn_cred.log`（走了哪条路径 / 耗时 / 成败 / 失败原因，超 64KB 自动滚动），"时不时失败"从此可事后定位。
- **测试卫生**：`test_switch_race.py` 的商汤凭据文件全部改到**临时目录**（`tempfile.mkdtemp`），从机制上杜绝再次投毒真实配置；fixture 域名换成商汤真实域名；两个诊断脚本 `tools/diag_sn_cred.py` / `tools/diag_sn_ui_state.py` 同样隔离。

**四、验证**
- **慢路径 13.01s → 5.57s（-57%）**；快路径 0.20~0.61s（直连）。
- 新增 23 项断言（102 项全绿，原 79 项无回归）：域名校验/投毒归档、JWT exp 解析与容错、过期跳过直连、有效令牌不拉浏览器、续期窗口主动换证、Edge 发现、面板退出 syncing、失败带上次成功时间、force 保留、代次丢弃旧结果、异常复位按钮、凭据日志落盘。
- 真机端到端：重启后 `sn_cred.log` 记录 `token-direct 1.90s 直连成功`，面板无异常；`ball.pid` 单实例；**1 个逻辑实例在 Windows 上本来就是 2 个进程**（venv 的 `pythonw.exe` 是重定向 stub，真实解释器是它的子进程）——此前看到的"多实例"是虚警，已确认；顺手清掉了早前测试留下的 1 个孤儿实例。
- 排查工具：`tools/diag_sn_cred.py`（链路分阶段耗时）、`tools/diag_sn_ui_state.py`（失败/超时/连点场景的按钮与面板状态）、`tools/restart_app.py`（干净重启 + 打印进程与凭据日志）、`tools/diag_multi_instance.py`（进程谱系，可鉴定 stub/真进程）。

**五、留待确认**
- 真机曾出现「JWT 名义还有 125 分钟却返回 401」——说明凭据可能被服务端提前失效，**401 兜底续期不可去掉**（本轮保留并加强）。若日后频繁出现，建议进一步做 OAuth2 PKCE 的纯 urllib 续签（可把 5.6s 再压到 1~2s），但那需要逆向登录流程，风险较高，暂不做。
- 回归测试目前只在 `--selftest`/离线桩下跑，真自动（Playwright）路径靠 `tools/diag_sn_cred.py --slow` 手工验证。

**六、环境级问题：火绒把我们的 pyc 当木马杀（本轮最大干扰源，已定性）**
- 浅浅猫提供的一条拦截日志揭开真相：**火绒**（`C:\Huorong\Sysdiag\bin\HipsDaemon.exe`，注意它**不在** SecurityCenter2 里，所以只查 Windows Defender 永远查不到）把
  `token-stats\__pycache__\card_app.cpython-313.pyc` 判为 `Trojan/Python.ShellLoader.am`
  （病毒ID `D78FACEAE47CB091`，`mcs=100` 置信度 100%，`solid=1` 通杀特征），**操作=执行 → 已处理，删除文件**（并结束进程）。
- 从火绒的 `C:\ProgramData\Huorong\Sysdiag\log.db`（`HrLogV3_60` 的 `filemon` 表）解出完整记录：
  **当天共 12 次**（08:23:44 ~ 09:06:36），**12 次全部指向同一个 `card_app.cpython-313.pyc`**，
  触发命令全是 venv 的 `python.exe`（跑 `test_switch_race.py` / `python -c "import card_app"`），
  即**我自己的测试与诊断**。隔离区里能看到 4 份同 sha1(`4C96C32B…`) 的副本。
- **为什么是 pyc 而不是 .py**：`card_app.py` 里 `subprocess.Popen(…DETACHED|NO_WINDOW)`（隐藏窗口拉起子进程）、
  `ctypes` 的 `OpenProcess+TerminateProcess`（杀进程）、往启动文件夹写 VBS（持久化）——单看都正当，
  合在一个字节码里就是 shell-loader 画像。而 `.pyc` **只有被 `import` 才产生**（直接 `pythonw card_app.py`
  作为 `__main__` 不产生），所以运行期本体没被打到，**受伤的全是开发/测试回路**。
- **这解释了什么**：这段时间"命令返回 exit 0 但没有任何输出""进程莫名消失""结果时有时无"——
  都是进程被结束的表现，**属于环境干扰，不是代码 bug**；同时也放大了"时不时获取失败"的观感。
- **已做**：`test_switch_race.py` / `tools/diag_sn_cred.py` / `tools/diag_sn_ui_state.py` 开头加
  `sys.dont_write_bytecode = True`；新增 `tools/syntax_check.py`（只编译到内存，替代会写 pyc 的 `py_compile`）；
  验证：105 项回归全绿且**不再生成 `card_app.pyc`**，火绒命中数停在 12 不再增长。
- **待浅浅猫决定**：① 火绒**信任区**加入 `token-stats` 目录与 `C:\Users\a3564\.workbuddy\`（根治，需她操作）；
  ② 是否要我进一步收敛"木马特征"（`_kill_stale_instance` 的 `TerminateProcess` 改协作式退出、
  自启从启动文件夹 VBS 改计划任务/`.lnk`）——会动到启动/重启逻辑，需先确认。

### 2026-09-16 浅浅猫 × WorkBuddy（第45轮·续2：续签秒败回归 + 一键重启 Security validation 报错）
> 浅浅猫反馈：①"直接获取凭证失败了，一直好不了" ②"上次改 exe 后，一键重启报错：Security validation failure: parent process has different executable!"

**① 续签一直失败 —— 是第45轮自己引入的回归（认领）**
- 日志：token 10:06 过期 → 浏览器续签 → `relogin 已提交账号密码` → **1 秒后**报「未捕获到积分数据」，11:02~11:17 反复 8 次（其中几次是她在手动点⟳）。
- 根因：第45轮把登录提交后的固定 `wait_for_timeout(8000)` 换成 `_wait_data(25)`，而 `_wait_data` 一看到 URL 还含 `/login` 就立刻返回 —— 可提交后那一两秒**必然**还在登录页 → 从未真正等待 → 每次续签都秒败。
- 修复：新增 `_wait_leave_login(30)`（轮询等 URL 离开登录页），**先等离开登录页、再等额度响应**；30s 仍停在登录页则报「自动登录未成功（密码可能有误或出现了验证码）」，不再用含糊文案。
- 真机验证（token 恰好已过期，正好走全链路）：`已提交账号密码 → 已离开登录页 → browser 12.92s 抓取成功`，随后直连 0.39s。
- 复盘：方向没错，但**漏了"等待条件要在状态真正改变后才成立"** —— 固定等待虽然笨，至少给了跳转时间。

**② 一键重启报错 —— PyInstaller 6.22 引导器的父进程校验**
- 这句报错不在我们源码里；对 exe 做 ASCII 扫描也找不到（只有 3 条别的 Security validation 串）。最后在 PyInstaller 6.22 **调试版引导器** `runw_d.exe` 里以 **UTF-16LE** 编码找到了它（宽字符，所以之前漏了）。
- 机制：PyInstaller 6.x 的 onefile **子进程**会校验「父进程可执行文件是否与自己一致」，不一致就弹这个错并拒绝启动。我们的链路把引导器注入的 `_PYI_*` 环境变量一路继承下去，一键重启时是 `pythonw.exe → 拉起 TokenStats.exe`，父进程对不上 → 拒绝。
- 修复：**拉起子进程时剥掉引导器环境变量**（`_PYI_APPLICATION_HOME_DIR / _PYI_ARCHIVE_FILE / _PYI_PARENT_PROCESS_LEVEL / _PYI_SPLASH_IPC / _MEIPASS2 / PYINSTALLER_*`）。两处都改：`card_app._restart_app()`（一键重启）+ `launcher.py`（exe→pythonw，不让它继续往下传）。
- 验证：105 项回归全绿；重建 exe（8.2MB，图标 RT_ICON×7+RT_GROUP_ICON×1）；**模拟一键重启**（与 `_restart_app` 同参拉起）→ 新实例接管 ball.pid(17748→11984)、旧实例退出、无报错、单实例 2 进程；接管后连续 3 次直连成功（0.28~1.05s）。火绒侧：重建的新 exe 未被点名（ShellLoader 命中仍 12 次，0 次涉及 exe）。
- **遗留**：浏览器续签耗时波动大（5.6s~66.3s 都出现过），怀疑与控制台 SPA 冷启动/杀软扫描有关，暂不追（有主动提前续期兜着，用户很少撞上）。

### 2026-09-16 浅浅猫 × WorkBuddy（第46轮：审查清单优化——安全/正确性/体验/布局 + 两卡合并）
> 浅浅猫给出一份完整审查清单，小蓝逐项核实后采纳 P0×3 + P1×3 + L3 + P2×1 + L1 合并，共 8 项。
- **P0 安全/正确性**：① `_sn_direct_fetch` 原 `insecure=True` 默认跳过 SSL 校验 → 改默认安全，仅证书错误时降级重试一次；② `src_head` 主题色不刷新 → apply_styles 补一行；③ `_sn_events_all` mtime 秒级 → 毫秒（`st_mtime_ns // 1e6`）。
- **P1 体验/性能**：④ ModelRow 悬浮提示加 50ms 节流（鼠标扫过 CPU 飙高）；⑤ StackedBarChart 数据末日期未延展时保留滚轮缩放/平移（不再切尺寸/刷新丢视图）；⑥ subtitle 加 `QSizePolicy.Ignored + MinimumWidth(0)` 防挤压时间按钮。
- **L3 布局**：暗色侧栏选中态 `qrgba(BLUE, 30)` → `55` 提升对比度。
- **P2 安全**：`sn_account.json` 密码改 base64（`password_b64`），新增 `_sn_account_credentials()` 统一解密，兼容旧明文；三处读取点（_sn_account_ready / 自动登录 / _load_account）全部切换。
- **L1 两卡合并**：SNSyncPanel + SNAccountCard 合并为一张「商汤同步」卡——顶部小细条+标题+状态徽标，中间分段切换按钮（🔐 账号密码 / 📋 手动 cURL），下方 QStackedWidget 两个表单页；删除 SNAccountCard 类；curl 输入框 34→52 加高；测试依赖 status_lbl/_status_mode/_sync_test 全部保留。
- 验证：分段切换/解密/base64 读写/深色主题冒烟 OK、**ALL 105 CHECKS PASSED**。

### 2026-09-17 浅浅猫 × WorkBuddy（第47轮：V3 补充素材 24 帧入库 + 点击分区）
> 浅猫：①新素材两张补充 V3（"注意之前的教训，不能直接切分"）②识别动作建议场景
> ③"我想把点击限定一个范围——底部 2/3 触发点击动作，顶部 1/3 触发摸摸头"，任意位置双击都打开面板。

**一、先核查源文件（第一步就没走弯路）**
- 两张**已经是透明背景的 RGBA PNG**（四角 alpha=0，全透明占 40~44%）→ **不需要抠图**，只切分+裁剪。
- 差点犯的错：图2 透明区有"蓝色噪点纹理"观感、图1 透明区带彩色 RGB（std≈29），我一度准备写"清理噪点"。
  量完才发现：图2 超出角色 4px 的半透明像素 **100% 是 alpha=1~2**（0.4% 不透明度，不可见）；
  图1 的透明区是 RGB 垃圾值而 **alpha=0**，且现有 V3/V4 帧完全一样（属正常）。
  → **一个像素都没动**。已把这条补进 `matting-forensics` 技能。

**二、为什么不能竖切（实证）**
- 图1 第 2 行：**6 个角色只有 4 个"干净空隙"** → 任何竖线都会裁断角色。
- 两张图各 12 角色且都是独立连通域（宽度比 0.90~1.14，无粘连）→ 走**连通域切分**。
- 自检：24 帧每帧恰好 1 个本体级连通域，全部 OK（没裁断、没带邻居）。
- 编号：行优先，`A01~A12`=图1、`B01~B12`=图2。

**三、动作识别（本轮确认：这环境里 Read 读 PNG 是真能看见图）**
- 已用于识别 24 个姿势、校对编号、对照现有 V3 素材。详见 `docs/V3补充素材清单.md`。

**四、入库（按浅猫 09-17 的分配，A07 剔除）**
- `sidle` +9（A05 A06 A08 A09 B05 B06 B07 B08 B11）→ 3→**12**
- `idle` +3（A02 A04 B04）→ 5→**8**；`click` +8（A01 A03 A11 A12 B01 B02 B09 B10）→ 4→**12**
- `pat` +3（A10 B03 B12）→ 3→**6**；`sleep`/`wake`/`drag` 不变 → 全量 **24→47 帧**
- 脚本 `tools/install_pet_v3_add.py`（只新增不覆盖）+ 映射表 `installed_map.json` + 检视页 `docs/pet_v3r_review.png`
- 三组"点击/摸摸头/特殊待机"都是 `random.randrange(len(pool))` **随机取一帧**，加帧只增加变化；
  `idle` 走 `_pet_idle_seq(n)`，第 0 帧仍是默认待机（新增 3 帧只会被偶尔随机插入）

**五、点击分区（新交互）**
- `_pet_click_region(y)`：**角色顶部 1/3 → 摸摸头(pat)**，**底部 2/3 → 点击互动(click)**；
  双击打开面板的原行为保留（两种形态都测了）。
- **分界取"实际绘制区域"的 1/3 而不是窗口的 1/3** —— 矮宽的帧（如躺姿）只画在窗口下半部分，
  按窗口算会把"角色头顶以上的空白"判成摸头。实测 v3 素材下分界落在窗口高 **35%**，贴近 1/3。
- 新增常量 `PET_PAT_TOP_RATIO`；`paintEvent` 记录 `_pet_draw_rect` 供判断；菜单/悬浮提示文案同步更新。

**六、验证**
- 回归 **105 → 117 项全绿**（新增：v3 帧数、`_pet_idle_seq(8)`、47 帧加载、分区边界（含正好 1/3 归点击）、
  无绘制区回退、双击开面板（桌宠+悬浮球）、新入库 22 帧连通域自检）
- 无头自检：v3 加载 47 帧、桌宠 118×148 渲染非空、绘制区 y=6~144、摸头分界 y=52
- 已重启实机；`docs/pet_v3r_review.png`（47 帧按场景分组）与 `V3补充素材_检视页.png` 放到桌面便于目视

**七、待确认**
- **B05（揉眼睛）**：原话"A05060809,B0605 可以当做特殊待机"，按字面把 `B0605` 理解为 B05+B06 都进特殊待机；
  若它该归摸摸头，换目录即可。
- 并行会话提醒：`816cc38 第46轮`（SSL 默认安全、L1 两卡合并等）来自**另一个会话**，动 card_app.py 前先看 git log。

### 2026-09-17 浅浅猫 × WorkBuddy（第48轮：click 退役 2 帧 + 检视图归位工作空间）

> 浅猫：①"这两张检视图从桌面移除到工作空间，以后新的图统一放工作空间，不要放桌面"
> ②"点击效果里第三第四张图效果不理想，移除到备选素材库" ③"这一版做完提交一下"

**一、检视图归位（桌面 → 工作空间）**
- 先核哈希：桌面 `V3补充素材_检视页.png` / `V3现有素材_对照页.png` 与 `docs/pet_v3_add_review.png` /
  `docs/pet_v3r_review.png` **内容完全一致**（`a957b8a4…` / `81e4cb62…`）→ 桌面那两份是纯副本。
- 用 `os.replace` **移动**回 `docs/`（不是删除），桌面已无 V3 相关图片。
- **约定**：以后检视图/素材类图片一律落 `docs/`，不再往桌面放副本。

**二、退役 `click_03` / `click_04`**
- 指认前先把歧义摆出来问：按**场景序号**数 = `click_03`（眨眼竖手指）+ `click_04`（惊讶张嘴，**原有老帧**）；
  按**本轮新加的 8 张**里数 = `A11` 举星星 + `A12` 提裙行礼。浅猫确认是**前者**。
- 退役 = **移动到备选素材库**（`assets/pet_alt/click/old_click_03.png` 等，文件原样保留，可随时取回），
  不是删除；索引 `assets/pet_alt/index.json` 记录原路径/时间/原因。
- 剩余帧**重排为连续编号**：`click` 12 → **10 帧**，全量 47 → **45 帧**；
  `installed_map.json` 自动同步 8 条映射并追加 `renumber_log`（A01 现在 = `click_03`）。
- 新工具 `tools/retire_pet_frames.py <场景> <编号...> [--dry] [--reason=...]`，以后退役任何帧一条命令即可。

**三、做这件事时踩到的两个点（都已写进文档与断言）**
1. **重排编号必须两步走**（先 `.rtmp` 再目标名）——`click_05→click_03` 会撞上还没让位的 `click_03`。
2. **重排后"同名文件≠同内容"**：回归里"退役帧已不在主库"我一开始按**文件名**判，直接被现实打回
   （`click_03.png` 还在，只是内容换成了 A01）。改成**比内容哈希**才成立。
   同时把"新入库帧连通域自检"从**硬编码编号**改成从 `installed_map.json` 反查 —— 否则编号一漂移断言就失效。

**四、验证**
- 回归 **117 → 119 项全绿**（新增：备选库留存 2 个退役帧可随时取回、退役帧内容已不在主库）
- 无头自检：v3 加载 **45 帧**（idle8/sleep3/wake2/drag4/click10/sidle12/pat6）
- 已重启实机（`ball.pid=29424`）；`docs/pet_v3r_review.png`（45 帧）、`docs/pet_click_review.png`（10 帧，
  标签带 `←A01` 来源标注）、`docs/retired_click_review.png`（退役核对图）

**五、顺带观察（凭据链路，未改代码）**
- 重启后正好撞上**瞬时 401**：`token-401 重试 1/2 → 2/2` → `token-direct 17.58s 已失效 → 浏览器续期`
  → **`browser 50.58s 抓取成功`**，自愈链路完整走通（这正是第45轮加的重试+续期逻辑在起作用）。
- 但有两处耗时偏大：快路径两次 401 重试合计 17.6s、浏览器续期 50.6s。
- 另一个观察：续期后落盘的 token `iat=03:32:21`，**比续期前那份的 exp（06:58）更早过期（06:32）**——
  与登录态 localStorage 里的值一致，即页面自己在用的就是这个 token，属商汤会话 token 的自然行为，
  不是我们读错；但会带来"续期完不久又进提前续期窗口（<15min）→ 再走一次 50s 慢路径"的抖动。
  当前 token 直连实测 **可用**。是否要为此加"不覆盖更优 token"的保护 + 继续压慢路径，**待浅猫定**。

### 2026-09-17 浅浅猫 × WorkBuddy（第49轮：新素材饱和度优化——先出样张待确认）

> 浅猫：新入库的素材"相比原 V3 素材颜色整体偏淡"，要做饱和度提升；先做一两张自查效果
> （"可以通过对比头发处的颜色"），给对比图，确认后再做全量并替换。

**一、量化"偏淡"（判据 = 头发色）**
- 取每帧 alpha 求 bbox → **顶部 45% 区域**（头的位置，避开裙摆/道具）→ 挑蓝色系像素（H∈[130,185]）
  → 统计 S/V。结果：

| 组 | 帧数 | 头发 S 中位 | V 均值 |
| --- | --- | --- | --- |
| 原 V3 帧（目标） | 22 | **117** | **158.5** |
| 09-17 新素材帧 | 23 | **95** | **148.5** |

- → "偏淡"是**饱和度低 22 + 明度低 10** 两件事，只补饱和度会留下"颜色对了但发暗"。

**二、做法与自检**
- numpy 浮点自做 RGB↔HSV（不用 `ImageEnhance.Color` / PIL 8bit HSV 往返，避免量化误差累积）；
  只改 `S'=S×k`、`V'=V+a·V·(1−V)`，**alpha 原样**。
- **往返自检**：`k=1.0, a=0` 输出与原图**逐像素完全相同**（最大差 0）→ 转换可信。

**三、踩到的关键坑：线性提亮会整片过曝**
- 线性 ×1.05 会把 **245~253 的像素 100% 推到 255** → 高光溢出率 **2.30% → 16.29%**
  （白围裙/发丝/眼睛高光丢层次）。
- 改用**平滑提亮** `V'=V+0.16·V·(1−V)`：中间调提亮最多、高光与极暗部几乎不动 →
  过曝率回到 **2.30%（与原样持平）**，而明度照样补上。

**四、方案对比（全量 23 帧预演）**

| 方案 | S 中位 | V 均值 | 高光溢出 | 头发中位色 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 原 V3（目标） | 117.0 | 158.5 | 3.22% | `#536AA0` | — |
| 新素材原样 | 95.0 | 148.5 | 2.30% | `#5C6B95` | 问题现场 |
| A `k=1.20` | 114.0 | 148.7 | 2.30% | `#516395` | 仍发暗 |
| **B `k=1.20`+平滑提亮** | **114.0** | **157.8** | **2.30%** | **`#576A9F`** | ✅ **推荐** |
| C `k=1.20`+线性提亮 | 114.0 | 156.0 | **16.29%** | `#55689C` | ❌ 过曝弃用 |
| `k=1.35` | — | — | — | `#445A95` | ❌ 过头 |

- 单帧逐通道验证（B vs 参照）：`idle_06` `#52679F` vs `#52699E`（ΔRGB 0/2/1）；
  `click_10`（起点最低 S=92）`#596B9E` vs `#546A9F`（ΔRGB 5/1/1）。

**五、产物与复现**
- 工具：`tools/analyze_pet_saturation.py`（量化，含 `--root=` 评估实验区）、
  `tools/enhance_pet_saturation.py`（增强，`--k`/`--lift`/`--v`）、
  `tools/review_pet_saturation.py`（对比图：完整帧+头部放大+头发中位色块+过曝率）
- 对比图：`docs/pet_saturation_compare.png`（idle_06）、`docs/pet_saturation_compare_click10.png`
- 实验产物 `assets/pet_v3_sat/<档位>/…`；**主库素材一个字节都没改**
- 文档：`docs/素材饱和度优化.md`

**六、待浅猫确认后执行**
- 采用 B 方案（或 A / 调参）→ 全量替换：**先整目录备份** → 只替换这 23 帧新素材
  （**不动 22 帧原 V3 素材**，它们是"目标"）→ 跑回归 → 重启桌宠（素材需重启才加载）。

**七、已应用（2026-09-17 07:08，浅猫确认后执行）**- 她的回复："你推荐的那一版效果很棒，可以继续了" → 按 **B 方案**全量替换。
- 备份：`assets/pet_v3r_bak_preSat_20260917_0708/`（**整目录 45 帧**）；
  替换 23 帧新素材，**22 帧原素材逐帧哈希与备份一致（零改动）**。- **幂等标记** `assets/pet_v3r/_saturation.json`：同参数重复执行直接跳过 ——
  没有它每跑一次就再叠一层饱和度，连跑两次颜色就毁了；`--force` 重做时**从备份取原图**再增强。
- 替换后实测：新素材头发 **S 中位 95 → 114**（目标 117）、**V 均值 148.5 → 157.8**（目标 158.5）。
- 回归 **119 项全绿**；v3 加载 45 帧；已重启桌宠（`ball.pid=27504`）；
  `docs/pet_v3r_review.png`（45 帧）与 `docs/pet_click_review.png`（10 帧）已重新生成。
- 工具 `tools/apply_pet_saturation.py`；回退方式见 `docs/素材饱和度优化.md` 第六节。

### 2026-09-19 浅浅猫 × WorkBuddy（第50轮：多机数据源）
> 浅猫："我们有多台电脑，但数据都是分开的，想增加别机数据源补充和导出本机数据的功能……导出包含当前两个来源分别全部的数据，导入时不影响别机原本的数据，按数据源自动识别并在程序侧补充上去，支持多台机器（可以命名机器），还可以选择覆盖更新某台机器的旧数据；新增一个 tab 专门放这个功能，页面不只有数据源管理，还有简单分机器的统计展示。"

**一、先摸清现状再动手**
- 通读 `card_app.py`（导航/渲染/刷新链路）、`scanner.py`（`scan_full` 数据模型）、`load_dsh_stats`（DSH 结构），确认三源数据已是 `{models, daily, dailySessions, today, firstDay, lastDay}` 同构 → 导出/合并在数据结构层是可行的。

**二、实现**
- 新增 `peer_store.py`：导出包（`token-stats.peer/1`）、机器名安全化、peers 存档（导入=按机器名分别存放、同名覆盖不翻倍）、`merge_machines`（按来源 + 按机合并）。
- 新增 `MultiMachinePage`/`MachineRow`：数据源管理 + 按机统计 + 别机列表（覆盖更新/删除）。
- CardWindow 加第 4 导航「🔀 多机合并」；导入/导出走 QFileDialog，删除/改名走 QMessageBox/QInputDialog。

**三、本轮踩到并修掉的真 bug**
- `machine_summary` 原只累加 `raw["models"]`，而 **`load_dsh_stats()` 根本不返回 models**（只有 daily）→ DSH 用量被整块漏算。改为统一走 `normalize_source` 后统计。
- 是我把测试断言写严之后才暴露的（第一版断言 `total=1500` 恰好漏过 DSH），**再次印证：断言要覆盖真实结构，不能只对着实现写**。

**四、依赖补齐（本机 venv 原缺）**
- 回归测试 §8/§9 依赖 `numpy / Pillow / scipy / playwright`，本机新 venv 全无 → 已逐个装上（playwright 是商汤真自动链路必需）。
- 环境坑：F 盘中文路径下删除文件会被安全删除钩子拦截（trash 对中文路径乱码失败）→ 临时文件先 Move 到 C 盘 Temp 再删。

**五、验证**
- `tools/syntax_check.py` 56/56 OK；**ALL 152 CHECKS PASSED**（119 → 152，新增 33 项多机断言，原功能零回归）。
- 待浅猫实机验证：多机页观感、导出文件在另一台电脑导入后的合并展示、覆盖更新/删除交互。

### 2026-09-19 浅浅猫 × WorkBuddy（第51轮：多机页视觉重做 + 统一自绘按钮/弹窗）
- 浅猫："功能上没问题，但视觉太简陋了，需要大幅优化、风格统一、做得精致协调，特别是弹窗和侧边 tab 栏的按钮太丑了；弹窗需要复用程序实现过的自绘形式，内部按钮/布局/呈现继续优化。"
- 建立**统一视觉基座**（而非逐个补样式）：按钮工厂 `make_btn/style_btn`（primary/ghost/danger/icon 四档）、自绘弹窗 `GlassDialog`+`DlgIcon`+`dlg_confirm/notify/prompt` 三工厂、`NavButton` 改纯自绘（3px 选中竖条 + 22px 图标槽对齐）。
- **多机页重做**：`MachineRow` 两行式 + `SrcBadge` 圆点胶囊；`StatRankRow` 名次徽标/渐变排行条/列头 `StatHeaderRow`（共用 `RANK_COL` 几何常量）；页面改「顶部操作卡 + 13:9 双栏」。
- **修掉 3 个真 bug**：① `btn_nav_multi` 漏出样式循环（第50轮按钮一直没统一背景）；② `set_dark/set_glass` 只 `update()` 不重算 QSS → 切暗色后子页面文字色残留（浅字压浅底不可读），新增 `_refresh_subpages_theme()`；③ `GlassDialog` 误用 `QDialog.setModal` + `_build` 漏传 `icon`。
- 沉淀**offscreen 截图复核方法论**：需手动注册 `msyh.ttc` 等系统字体；`widget.grab()` 会把圆角外透明区合成 `#efefef` 灰，不可据此判卡片底色；主题切换类截图必须在建窗口前设好 `theme_state`。
- 回归 **ALL 192 CHECKS PASSED**（152 → 192，新增 §11 视觉基座 40 项，零回归）；syntax_check 56/56 OK。

### 2026-09-19 浅浅猫 × WorkBuddy（第50轮：多机数据源 · 详细记录）
  - **背景**：用户有多台电脑，各机数据互相独立。需求：① 导出本机数据（含 WB + DSH 两个来源的全部数据）；② 导入别机数据时**不影响本机原有数据**，按数据源自动识别并补充；③ 支持多台机器并可命名；④ 可选择覆盖更新某台机器的旧数据；⑤ 新增独立 Tab，含数据源管理 + 分机器统计展示。
  - **新增 `peer_store.py`（纯标准库，~400 行）**：
    - **导出包格式** `token-stats.peer/1`：`{format, version, app, exported_at, machine, sources:{wb,dsh}}`；`build_export_package` 只收录无 `error` 的来源。
    - **机器名安全化** `_sanitize_machine`（去路径分隔符等，防文件名注入），空名兜底 `unknown-machine`。
    - **peers 存档**：`peers/<machine>.json` + `peers/index.json` 注册表（记录 imported_at/sources/digest）。`import_peer(replace=)` 中**同名机器重复导入=覆盖该机快照**（全量快照语义，避免同机重复导入翻倍），不同机器各自独立累计。
    - **`merge_machines(entries, include_local, peer_filter)`**：逐来源 `normalize_source` 后合并 → `daily/models/dailySessions` + `machines`（按机摘要）+ `bySource`（按来源拆分）。支持"仅本机 / 含别机 / 只筛某台"。
    - **DSH 结构适配**：`load_dsh_stats()` 只给 `daily`、**没有 `models`**，故 `_normalize_dsh` 现算 models，`machine_summary` 也走 normalize（否则 DSH 用量会被整块漏掉——本轮踩到并修掉）。
  - **新增 UI `MultiMachinePage` + `MachineRow`**（card_app.py，插入 SNQuotaPage 之后）：
    - 卡1「数据源管理」：本机机器名（可改名）+ 导出本机数据 / 导入别机数据按钮 + 说明文案。
    - 卡2「按机统计」：每台机一行（🖥 本机 / 💻 别机 + 机器名 + 比例条 + token 总量 + 请求 + 日期区间），按总量降序，顶部合计。
    - 卡3「已导入的机器」：每台机一行（机器名 + 来源徽标 + token/请求/日期/导入时间 + **覆盖更新** / **删除** 按钮）。
  - **CardWindow 接入**：侧栏新增第 4 个导航「🔀 多机合并」（`source == "multi"`，stack index 2）；`refresh` 走后台线程 `_build_multi_stats()`（本机实时 scan_full + DSH + 全部别机快照 → 合并）；导入/导出用 `QFileDialog`，删除/改名用 `QMessageBox`/`QInputDialog`，结果用 `_show_toast` 反馈。
  - **首次进入策略**：`load_initial` 的 multi 分支先渲染轻量视图（仅别机列表 + 本机名），完整合并交给随后的后台 refresh，避免切页卡顿。
  - **本轮修正**：`machine_summary` 原只累加 `raw["models"]` → DSH（无 models 字段）整块漏算；改为走 `normalize_source` 统一结构后统计（回归断言的严格化暴露了它）。
  - **测试隔离**：回归测试新增 §10（33 项断言）——导出包构建/校验/坏包拒绝、合并累加与按来源拆分、别机筛选、DSH normalize、机器名安全化、导入/覆盖/删除、多机页面渲染；`peer_store.PEERS_DIR/PEERS_INDEX` 同步隔离到临时目录。
  - **验证**：`tools/syntax_check.py` 56/56 OK（+ peer_store.py 单独检查 OK）；**ALL 152 CHECKS PASSED**（原 119 项无回归，新增 33 项）。

- **2026-09-19 第51轮：多机页视觉重做 + 全程序按钮/弹窗语言统一**
  - **背景**：第50轮功能可用但"视觉太简陋"——弹窗是系统 `QMessageBox`/`QInputDialog`（字体圆角与卡片完全不搭）、侧边 tab 按钮扁平、多机页布局与内部按钮不协调。用户要求：复用程序已实现的自绘形式，统一视觉风格，精修布局与呈现。
  - **新增统一视觉基座（card_app.py）**：
    - **按钮工厂** `make_btn(kind, text)` / `style_btn(btn, kind, pt, height)`，四档语义：`primary`（实心蓝，主操作）/ `ghost`（描边，次操作）/ `danger`（hover 转红，破坏性）/ `icon`（无边框轻量）。
    - **自绘弹窗体系** `GlassDialog(GlassPodFrame)` + `DlgIcon` 语义圆形徽章 + 三个工厂函数 `dlg_confirm` / `dlg_notify` / `dlg_prompt`；同款圆角 + 菲涅尔描边 + 可拖拽标题区 + `Esc`/`Enter` 快捷键 + `add_field()` 输入框 + 局部 `QEventLoop` 实现模态等待。
    - **`NavButton` 改纯自绘**（不再套 QSS）：左侧 3px 竖条选中指示器、固定 22px 图标槽（四个按钮文字左边界严格对齐）、选中/ hover 双态区分。
  - **多机页重做**：
    - `MachineRow` 改**两行式**（上：机器名 + 总量 / 中：来源胶囊徽标 / 下：请求·区间 + 按钮组），新增 `SrcBadge`（自绘圆点胶囊）替代原来的 `·` 文本拼接，按钮改 `覆盖更新`+`删除`（danger）。
    - `StatRankRow` 替代原裸进度条：**名次徽标**（金/银/铜/中性）+ 机器名（本机加蓝点、超长省略）+ 通栏渐变排行条 + 右对齐总量 + 底行请求/区间；配 `StatHeaderRow` 列头，两者共用 `RANK_COL` 几何常量保证像素级对齐。
    - 页面从「三张竖排卡」改「顶部操作卡 + 主体 13:9 双栏」（左按机统计 / 右已导入机器），新增 `MachineChip` 本机身份胶囊、`_panel_head()` 面板头工厂、空态占位组件。
  - **修掉的真 bug（3 个）**：
    - ① **`btn_nav_multi` 漏出样式循环**：`apply_styles` 里 `for b in (wb, dsh, sn)` 少写了 multi，第50轮新按钮一直没有统一背景样式。
    - ② **主题切换不重刷子页面**：`set_dark`/`set_glass` 只调 `update()`，**不会重算 QSS** → 切暗色后多机页/商汤页保留旧主题文字颜色（浅字压浅底，几乎不可读）。新增 `_refresh_subpages_theme()` 显式重刷。
    - ③ `GlassDialog` 初版误用 `QDialog.setModal()`（基类是 QFrame），且 `_build()` 未接收 `icon` 参数致 `NameError`。
  - **截图复核方法论（本轮踩坑沉淀）**：
    - `QT_QPA_PLATFORM=offscreen` 下 Qt **找不到系统字体** → 必须 `QFontDatabase.addApplicationFont("C:\Windows\Fonts\msyh.ttc")` 等，否则截图全是豆腐块。
    - `widget.grab()` 会把**圆角外的透明区合成成 `#efefef` 灰** → 不可据此误判卡片底色（曾误判"暗色卡片是白的"）；要验真实底色须 `grab()` 整个窗口再按坐标采样，或单独 `grab()` 目标控件看其颜色直方图。
    - 主题切换类截图**必须在建窗口前就把 `theme_state` 设好**，运行中切换会因事件时序抓到旧帧。
  - **测试**：回归新增 §11「视觉基座」（40 项断言）——按钮四档创建/覆盖样式、`GlassDialog` 结构/字段/快捷键/无取消按钮变体、三个 dlg 工厂签名、三个交互方法**已清除原生弹窗**（断言源码里不含 `QMessageBox`/`QInputDialog`）、`NavButton` 纯自绘与四按钮样式一致、`set_dark` 联动子页面、`StatRankRow` 比例/名次/本机标记、`RANK_COL` 常量、`SrcBadge`/`MachineChip` 自适应。
  - **验证**：`tools/syntax_check.py` **56/56 OK**；**ALL 192 CHECKS PASSED**（152 → 192，新增 40 项视觉断言，原功能零回归）。
  - **待浅猫实机验证**：多机页观感、暗色/亮色切换后文字对比度、自绘弹窗观感（导出/导入/删除/改名四处）、侧边导航选中态。

### 2026-09-19 浅浅猫 × WorkBuddy（第52轮：弹窗不可见根治 + 同步中切页 + 多机页单列重做 + 暗色框适配）

> 浅猫原文："同步过程中无法切换到多级合并页面，需要优化，然后机器改名功能不能用，修复（怀疑是弹窗有问题，因为其它的弹窗我也没看到）；另外双栏的观感并不好，我认为你还是需要好好规划好我们的统计需要如何呈现，然后就是统计里面的框框没有适配暗色模式，修复；然后就是我觉得已导入机器是不是可以和数据源管理卡片结合在一块，框框放到左边，然后导出导入按钮放到框框的右边，导入按钮需要写成导入/更新别机数据，如果是新机器的数据就导入，旧机器的数据就覆盖更新"

**一、四个真 bug（全部实测定位，非猜测）**

1. **所有弹窗根本看不见 → 根因：`GlassDialog` 基类选错**
   - 第51轮用 `GlassPodFrame(QFrame)` + 手写嵌套 `QEventLoop` + `Qt.Dialog` 标志"假装"模态。实测（写诊断脚本量测，不靠肉眼）：`IS_QDIALOG=False`、**`IS_MODAL=False`**、**`SIZE=640×480`** —— 640×480 是 `QWidget` 的**默认尺寸**，说明 `adjustSize()` 根本没算出 `sizeHint`（布局从未生效）。
   - 后果：Windows 下既不抢焦点、也压不住主窗 → **表现为"弹窗闪一下就没了/压根没出现"**。浅猫说"机器改名不能用、其它弹窗我也没看到"，就是这一个根因。
   - **修复**：基类改真 `QDialog`，模态走**原生 `exec()`**；玻璃底面把 `GlassPodFrame.paintEvent` 里的绘制逻辑抽成独立函数 **`paint_pod(widget, radius, inset)`**，让 `QDialog`（无法再继承 `QFrame`）与 `GlassPodFrame` 共用同一层玻璃；尺寸由 `_fit()`（`setFixedWidth` + `layout().activate()` + `adjustSize()` + 居中）。
   - 修后实测：`IS_QDIALOG=True`、`IS_MODAL=True`、`SIZE=380×111`、`EXEC_RESULT=True`，并删掉 `_loop` / `accept()` / `reject()` / `closeEvent` 这套手写模态残留。
   - **沉淀结论**：Qt 里"真模态"只能靠真 `QDialog` + `exec()`；`QFrame` + 事件循环 + `Qt.Dialog` 标志在 Windows 上不可行。

2. **同步进行中无法切到多机合并页 → 根因：切页耦合在数据回调里**
   - `setCurrentIndex` 只写在 `render()` 里，而 `render()` 由后台扫描回调触发。用户在**同步中途**切页时，`refresh()` 因 `_scanning=True`（合并重复请求）直接 `return`，回调被推迟 → **stack 一直停在旧页**。
   - **修复**：抽出 **`_apply_page()`** —— 纯视图方法，只做 `setCurrentIndex` / `setVisible`，不碰数据；`load_initial()` 与 `render()` 都在最前面调它。切页从此**瞬时完成、与数据到达无关**。回归里专门造了"`_scanning=True` 时切页"的场景断言。

3. **统计里的框框没适配暗色 → 根因：玻璃底双层叠加**
   - `MachineRow` 当时继承 `GlassPodFrame`，但父级 `peer_card` **本身就是** `GlassPodFrame` → 两层玻璃底叠加，内层明显比外层深/亮，暗色下尤其突兀（外壳浅灰、内层近黑）。
   - **修复**：`MachineRow` 改普通 `QWidget`，自己只画**一层**中性底（`ZEBRA` / hover `HOVER`）+ 细描边（`BORDER`），颜色全部走全局调色板。

4. **（修复过程中发现）滚动条在暗色下仍是浅色 → 根因：局部 QSS 顶掉全局 QSS**
   - Qt 层叠规则：对某 widget 调 `setStyleSheet` 会**替换**（而非叠加）该子树继承的 app 级样式。给 `QScrollArea` 设"透明底"时，顺手把全局 `QScrollBar` 样式一起干掉了。
   - **修复**：新增 `scrollbare_qss()` + `style_scroll_area(area)`，把滚动条规则与透明底规则**打包一起设**，并在 `apply_theme()` 里重套（否则切主题后滚动条色残留）。

**二、多机页体现重做（观感问题）**

- 从「顶部操作卡 + 13:9 双栏」改为**单列纵向三段**，每段独占整行宽度：
  1. **数据源管理** —— 左侧 `MachineBox` 机器框（占满左侧、可拉伸、内嵌右侧「改名」热区），右侧「⬆ 导出本机数据」「⬇ 导入/更新别机数据」两个按钮；
  2. **按机统计** —— 通栏，`StatHeaderRow` 列头 + 按总量降序的 `StatRankRow`；
  3. **已导入的机器** —— 通栏，每台别机一行（来源徽标 / 总量 / 请求 / 区间 + 覆盖更新 / 删除）。
- **`MachineChip` → `MachineBox`**：原胶囊只够放一个名字，放不下"改名"入口，改为带明确边界、`Expanding` 的水平框，自绘显示器图标 + "本机"小字 + 机器名（超长省略）+ 右侧「改名」。
- 标题行右侧补 `peer_count_lbl`（"N 台"）与 `peer_scope_lbl`（"N 台别机"），空态提示与按钮同源。
- `StatRankRow` 补非 hover 态的**斑马纹底色**（`ZEBRA` / `CARD` 交替），长列表更易横向对齐阅读。

**三、导入/更新语义对齐（按钮文案与行为）**

- 按钮文案：`导入别机数据` → **`导入/更新别机数据`**（对话框标题、机器名不匹配的提示语、空态提示全部同步改）。
- 一个按钮覆盖两种情形，反馈措辞明确区分，避免用户误以为旧数据被累加：
  - 包内机器名**从未出现过** → 「**新机器已导入**」+ "该机器名此前未出现过，已作为一台新机器加入统计。"
  - 包内机器名**已存在** → 「**已覆盖更新同名机器**」+ "该机器名已存在，本次按最新快照覆盖其旧数据（**不是累加**）。"
- 语义本身由 `peer_store.import_peer` 保障（同名机器=全量快照覆盖），本轮只在 UI 层把 `info["replaced"]` 映射成上面两句准话。

**四、验证**

- `tools/syntax_check.py` **56/56 OK**。
- `test_switch_race.py` **ALL 224 CHECKS PASSED**（192 → 213 → 224）。新增：
  - §11 补 `GlassDialog` 真模态/真尺寸/已移除 `QEventLoop`/复用 `paint_pod`；`MachineBox` 拉伸与改名信号；按钮文案；滚动条规则；`MachineRow` 不再继承 `GlassPodFrame`。
  - **§12 切页即时性**（9 项）：`_apply_page` 存在、`render()` 调用它、`_scanning=True` 时切 multi/sn/wb 的 stack index 与区间控件可见性、`_apply_page` 为纯视图（断言源码里不含 `self.refresh` / `_render_multi_page`）。
  - **§13 导入/更新语义**（11 项）：UI 两种文案与 `replaced` 映射、对话框标题一致、`peer_store` 同名再导入仍是一台且**整体覆盖（100 → 999 而非 1099）**、不同机器名并存。
- 截图复核：亮/暗 × 默认/大窗口多机页、三种弹窗变体、整窗亮/暗对照（`docs/assets/previews/_r52b_*.png` / `_r52c_*.png`）。
- **待浅猫实机验证**：同步中切页、改名/删除/导出/导入四处弹窗、暗色下统计区框框、新的单列三段布局。

---

### 2026-09-19 浅浅猫 × WorkBuddy（第52轮 · 交接要点）

- **改的文件**：`card_app.py`（主要）、`test_switch_race.py`、`README.md`、`协作进度.md`。`peer_store.py` 本轮未改（同名覆盖语义第50轮已实现）。
- **三条硬约束（改代码前务必看，都是踩过的坑）**：
  1. `GlassDialog` **必须是 `QDialog` 子类**，模态只能用原生 `exec()`；用 `QFrame` + 手写 `QEventLoop` 在 Windows 上弹窗不可见。玻璃底统一走 `paint_pod(widget, radius)`。
  2. **禁止玻璃底嵌套**：`GlassPodFrame` 里不要再放 `GlassPodFrame` 当行容器，两层底叠加会串色（暗色下尤其明显）。行容器用 `QWidget` 自绘一层底。
  3. 对 widget 调 `setStyleSheet` 会**替换**该子树继承的全局 QSS → 涉及滚动条请用 `style_scroll_area()`，并在 `apply_theme()` 里重套。
- **一个架构约定**：`_apply_page()` 是纯视图方法（只 `setCurrentIndex`/`setVisible`），`load_initial()` 与 `render()` 都要先调它；**不要把切页逻辑塞回数据回调**，否则同步中切页又会卡住。
- **改完必须跑**：`tools/syntax_check.py`（56/56）+ `QT_QPA_PLATFORM=offscreen <pyside6 venv>/python test_switch_race.py`（224 项）。

---

### 2026-09-19 浅浅猫 × WorkBuddy（第53轮：暗色输入框根治 + 内联改名 + 骨架占位 + 项目文件夹整理）

> 浅猫原文："弹窗输入框没有适配暗色模式，另外我决定改名不需要弹窗，直接在原有名字框就能输入修改然后保存会不会更好？然后按机统计已经有的机器数据条每次进入这个页面也还是要刷的一下出来，观感不好，需要优化，然后完成这次任务后，把我们项目文件夹整理一下，不管是根目录还是各个文件夹里只要最基本的；没有用的素材、旧的文件、网页全部都放到一个文件夹里整理好"

**一、弹窗输入框在暗色下仍是纯白 → 根因：`apply_theme()` 跑在 `add_field()` 之前**

- 先实测再动手（写 `_diag_input.py` 量测，不靠肉眼）：**亮色与暗色两种主题下**，输入框 `styleSheet` 长度都是 **0**，中心像素都是 **`rgb(255,255,255)`** —— 说明"给输入框套主题样式"那段代码**从未执行过**。
- 根因链：`GlassDialog.__init__` 末尾调 `apply_theme()`，而它的输入框样式分支判断 `if self._field is not None`；但 `self._field` 是 **`add_field()` 里才创建**的 —— `__init__` 执行时它还是 `None`，**分支被跳过**；之后 `add_field()` 建好输入框，却**没人再调一次 `apply_theme()`**。于是输入框一直是原生白底。
- **修复**：`add_field()` 建好 `QLineEdit` 后立刻 `self.apply_theme()`，并补 `self._fit()` 重算高度。`__init__` 加 `self._tag = None` 初值。样式同时补齐 hover / `:focus` / `selection-background-color` 三组规则。
- 修后实测：暗色背景 **`rgba(38,43,53)`**（全局 `TRACK`）、文字 **`#f0f3f8`**（全局 `TEXT`）；亮色背景 `rgba(237,241,247)`。

**二、（修复过程中发现）追加输入框后弹窗不长高，输入框被裁**

- 实测：`add_field()` 前 height **113**，加了输入框后仍 **113** —— 构造期的 `sizeHint` 只反映**当时已有**的内容，并把 `minimumHeight` **锁死**在该值上，`adjustSize()` 无法突破。
- 第一次修复只 `invalidate()` 外层 `QVBoxLayout` → 113 → **145**，仍不够（理想 160）。**根因**：输入框是加在 `_wrap_lay` 这个**内层 `QHBoxLayout`** 上的，只顶层的 `invalidate()` 不会让内层重算 `sizeHint`。
- **修复**：`_fit()` 里**逐层** `invalidate()+activate()`（先内层 `_wrap_lay`，再外层 `QVBoxLayout`），然后 `setMinimumHeight(0)` 解开旧锁再重设，最后 `adjustSize()` + 居中。
- 修后实测：`sizeHint` **113 → 160**，height **113 → 176**，show 之后输入框 `y=27` 不再被裁。

**三、改名改为就地内联编辑（按浅猫决定，取消弹窗）**

- `MachineBox` 重写：内嵌一个**常驻 `QLineEdit` 子控件**，平时 `hide()`；进入编辑态时 `paintEvent` 提前 `return` 让子控件露出。新增 `name_edited = Signal(str)`。
- 交互：点名字框 → 进入编辑态（预填当前名 + 全选）→ `Enter` / 点「保存」/ **失焦** 提交；`Esc` / 点「取消」放弃。空名或同名**不落盘、不发信号**。
- `_recalc_edit_btns()` / `_layout_edit()` 在 **`start_edit()` 里就调用**（不能等 `paintEvent`）—— 否则刚进编辑态立刻点「保存」会打空。
- 「保存/取消」为自绘按钮：保存 = hover 蓝色填充 + 白字，否则幽灵样式；取消 = hover 红调。
- `_on_rename_machine()` 不再调 `dlg_prompt()`，直接 `peer_store.save_machine_name(text)`。

**四、按机统计"刷的一下出来" → 骨架占位**

- 实测（`_diag_flash.py`）：进入多机页后，轻量首帧 `statRows` = **0**，等后台扫描回来时 **0 → 3** 突然长出 —— 就是浅猫说的"刷的一下出来"。
- **修复**：`render(..., pending=True)` 时改用 `StatRankRow(placeholder=True)` **骨架行**（画同高 70px 的暗色条块：rank 圆 + 84px 名字条 + 68px 总量条 + 30% 进度条 + 148px 页脚条），scope 文案显示「**正在汇总…**」。`_skeleton_n` 记住上一轮台数，让骨架与真实内容**同高**，切换不跳变。
- `load_initial()` 的多机分支置 `"pending": True`；`_render_multi_page()` 透传 `pending=bool(s.get("pending"))`。

**五、（修复过程中发现）列表重绘的资源泄漏**

- `_clear_layout` 原实现 `while lay.count(): takeAt(0).widget()` 有两个后果：
  1. `addStretch()` 插入的是 **spacer item**，`widget()` 返回 `None` → 永远排在最前，越积越多；
  2. `deleteLater()` 只是"稍后删除"，旧控件仍挂在 parent 上 → `findChildren()` 越数越多（实测骨架行渲染完 1 个后**一直残留**）。
- **修复**：逐项 `takeAt(0)`，`widget` 非空则 `setParent(None)` + `deleteLater()`。
- 修后实测：骨架行 **1 → 0**；反复 render 后 `stat_lay.count()==3`、`peer_lay.count()==2` 稳定不变。

**六、项目文件夹整理（浅猫确认的四个参数）**

- 归档位置：**项目内 `_archive/` 按类型分子目录**；桌宠 **4 版本并行保留**（`pet` / `pet_v2` / `pet_v3r` / `pet_v4`，对应 `PET_THEMES` 四项），**其余归档**；素材**只移明确的历史备份/中间产物**；`.gitignore` 一并整理。
- **共移动 98 项 / 137.0 MB**，`shutil.move`（同盘 rename，零拷贝零改写），**全程无任何删除**：

  | 分类 | 项数 | 体积 | 内容 |
  |---|---|---|---|
  | `_archive/assets-debug/` | 22 | 16.5 MB | 根级调试截图（`_debug_*` / `_v2_*` / `_v3_set*`） |
  | `_archive/assets-pet-legacy/` | 12 | 53.0 MB | 桌宠历史版本（`pet_v3_sat` / `pet_v5` / 各 `*_bak_*` / `*_src` 等） |
  | `_archive/docs-reviews/` | 15 | 58.1 MB | 历史审查页（7 个 `*_review.html` + QA 报告 + 审查截图） |
  | `_archive/root-legacy/` | 3 | 9.1 MB | 旧 exe 备份、`legacy/`、`%SystemDrive%/`（路径变量未展开误建） |
  | `_archive/tools-once/` | 46 | 259.9 KB | 一次性脚本（`diag_*` / `review_*` / `split_*` / `assemble_*` 等） |

- **归档前的关键拦截**：`test_switch_race.py` §8 **读取** `assets/pet_alt/index.json` 与 `assets/pet_v3_add/installed_map.json`（断言退役帧可检索、新帧单连通）—— 这两个目录原本在归档候选里，**移走会直接打断回归**。已加入显式排除表。
- **另一个拦截**：`git rm --cached` 的路径匹配**不能用裸前缀** —— `assets/pet_v2_alpha` 的前缀把**现役** `assets/pet_v2_alpha2_qc.json` 误判为已归档（52 项）。改成"目录必须整段匹配 / 文件必须精确相等 + `NEVER_REMOVE` 保护表"后修正为 **51 项**。
- 清理：删掉 2 个 `__pycache__`（根 + `tools/`，仅字节码缓存，可再生）。
- `.gitignore` 收敛：原先逐条列的素材/审查页排除规则**全部失效**（那些路径已不存在），收敛为一条 **`/_archive/`**；同时补 `/_*.txt`、打包中间产物（`/build/`、`/dist/`）规则。
- `git rm -r --cached` 移除 **51 个**已归档路径的索引（分两批，rc=0），**磁盘文件完整保留**；并逐项核对确认 14 个抽查样本全部在 `_archive/` 对应分类下找到。
- 新增 **`_archive/README.md`** 归档清单：五个分类逐项说明 + 保留/排除依据 + 取回命令 + git 追溯方法。
- 整理后项目体积：**112.7 MB**（归档区另 137.0 MB）。根目录只剩 21 个条目。

**七、验证**

- `tools/syntax_check.py` **全部通过（10 个文件）**。
- `test_switch_race.py` **ALL 268 CHECKS PASSED**（224 → 268）。新增 **§14 共 44 项**：暗色输入框调色板取值/深底色/浅文字、`add_field` 后高度重算、`MachineBox` 的 `name_edited` 信号与常驻输入框、`start_edit`/`cancel_edit`/`commit_edit` 全路径（预填、同名不发信号、空名不落盘）、编辑态按钮存在、`_on_rename_machine` 不再引用 `dlg_prompt`、`name_edited` 接线、骨架行首帧/清除/同高/scope 文案、反复 render 不累积行、`_clear_layout` 存在且在两个列表上使用。
- **归档后复跑**：syntax 全过 + **268 项全过** —— 证明归档未触碰任何被测试引用的路径。
- 截图复核 8 张（`docs/assets/previews/_r53_*`）：多机页亮/暗 × 骨架/就绪、机器框编辑态亮/暗、弹窗输入框亮/暗。
- **待浅猫实机验证**：弹窗输入框暗色表现、就地改名三种提交方式、多机页进入时统计条不再闪。

---

### 2026-09-19 浅浅猫 × WorkBuddy（第53轮 · 交接要点）

- **改的文件**：`card_app.py`、`test_switch_race.py`、`README.md`、`协作进度.md`、`.gitignore`；**新增** `_archive/README.md`；**移动** 98 项至 `_archive/`。`peer_store.py` / `scanner.py` 本轮未改。
- **三条新增硬约束（第 53 轮踩出来的）**：
  1. `GlassDialog.add_field()` 里**必须**补 `self.apply_theme()` —— `__init__` 的 `apply_theme()` 跑在输入框创建之前，样式分支永远进不去。
  2. `add_field()` 后**必须**经 `_fit()` 重算高度，且 `_fit()` 要**逐层** `invalidate()`（内层 `_wrap_lay` 先于外层 `QVBoxLayout`），并先 `setMinimumHeight(0)` 解旧锁。
  3. 清空 layout **必须**用 `_clear_layout()`（`takeAt(0)` + `setParent(None)` + `deleteLater()`）—— 只取 `.widget()` 会漏掉 `addStretch` 的 spacer，且 `deleteLater()` 延迟删除会造成子控件累积。
- **两条既有约束仍然有效**：`GlassDialog` 必须是真 `QDialog`（原生 `exec()`）；禁止玻璃底嵌套。
- **归档纪律**：新产生的历史物（调试截图、素材旧版、一次性脚本、废弃审查页）**直接放 `_archive/` 对应分类**，不要堆回项目目录；`_archive/` 已被 `.gitignore` 排除，无需再逐条加规则。
- **改完必须跑**：`tools/syntax_check.py`（10 个文件全过）+ `QT_QPA_PLATFORM=offscreen <pyside6 venv>/python test_switch_race.py`（**268 项**）。

---

### 2026-09-19 浅浅猫 × WorkBuddy（第54轮：从另一台机器同步第51~53轮改进 + 本机补归档）

> 浅猫："这是我在另一台机器上做好的完整新版本，请你阅读一下改进记录，并将改进文件同步到我们的项目里，然后提交"

**一、先核对同源性（这一步决定整个同步方式）**
- 两边 `bbf1f79` 是**同一个 commit 对象**（hash 相同）→ 同一条历史。
- 本机 HEAD = `bbf1f79`、那台机器 HEAD = `32f5b41`；本机**独有提交 0 个**、落后 6 个 →
  `git merge --ff-only` 零冲突零丢失。同步前已整目录备份：`../_backup/token-stats_preSync_20260919`。

**二、同步进来的 6 个提交**
`d68ec2e` 第51轮多机数据源合并（新增 `peer_store.py` + 「多机合并」Tab）→ `6a85fb2` 第51轮多机页视觉重做 / 统一自绘按钮弹窗 → `48932de` .gitignore 收尾 →
`1cad5df` 第52轮弹窗不可见根治 / 同步中切页 / 多机页单列 → `6085a4c` 第53轮暗色输入框根治 / 内联改名 / 骨架占位 / 项目文件夹整理 → `32f5b41` .gitignore 恢复排除。

**三、⚠️ 同步时踩到的坑（以后跨机同步同一仓库务必注意）**
- 那台机器的整理方案是「**移动 98 项到 `_archive/`，零删除**」，但**带回来的副本不含 `_archive/`**
  （体积大，留在了原机）。直接快进会让 git 清掉 46 个一次性工具 + 15 个历史审查页，
  而 `_archive/` 在 `.gitignore` 里不会跟着过来 → **这些文件会真的消失**。
- 实际检出时还多删了一层：**14 个"内容未变"的文件被目录级删除一并带走**
  （`tools/build_exe.py` 等 6 个保留工具、`docs/V3补充素材清单.md`、`docs/素材饱和度优化.md`、
  `docs/assets/previews/` 里 6 张旧图）。原因：git 对"索引与工作区内容相同"的文件**跳过写回**，
  所以先被误删后不会自动补回；在 `status` 里表现为 14 个 `D`。**处理：`git checkout -- .` 从索引恢复**。
- 结论：① 跨机同步"带磁盘整理"的仓库，要么把归档区一起带过来，要么在本机复刻归档后再取回被删文件；
  ② 同步后必须用「**备份 vs 当前磁盘全量对比**」查漏——git 只报它管的文件，被忽略的那批删了它不吭声。

**四、本机补归档（复刻那台机器的分类方案）**
- `_archive/` 五个分类共 **99 项**：`tools-once` 46 · `assets-debug` 22 · `docs-reviews` 15 ·
  `assets-pet-legacy` 14 · `root-legacy` 2；另写 `_archive/README.md`（分类说明 + 取回命令 + 现役保留清单）。
- 全程**只移动/复制，零删除**；被 git 删掉索引的 46+15 项从备份取回后归位。
- 项数核对：本机 99 vs 那台机器 98（差异来自磁盘状态细节），其中 22/15/46 与她的记录**完全吻合**。
- 现役素材保留：`pet` / `pet_v2` / `pet_v3r` / `pet_v4`（对应 `PET_THEMES` 四项）+ 退役帧库 `pet_alt`
  + 素材源 `pet_v3_add`（**后两个被回归测试读取，不能动**——这条拦截她那边也踩过）。

**五、验证**
- `tools/syntax_check.py` **10/10 通过**；`test_switch_race.py` **ALL 268 CHECKS PASSED**（与那台机器一致）。
- `git status` 干净；根目录从 43 项收敛到 21 项 + `_archive/`；归档区 130 MB。
- 唯一未清理项：根 `__pycache__`（沙箱 safe-delete 拦截，纯可再生字节码缓存，无害）。

**六、待浅猫实机验证（沿用第53轮清单）**
弹窗输入框暗色表现、就地改名（Enter / 保存 / 失焦）三种提交方式、多机页进入时统计条不再闪、
以及「导出本机数据 → 在另一台机导入后合并展示、覆盖更新、删除」的完整来回。

---

### 2026-09-19 浅浅猫 × WorkBuddy（第55轮：修复「导入的别机数据只在多机页可见」）

> 浅猫实机反馈："把那台机器的数据带回来实际测试后终于发现问题了，你可以看到目前我已经导入了数据，
> 但是只在多机页有数据，我当时的诉求是前面的页面也补充上这些数据"

**一、根因（数据链路只在一处接上了）**
- `refresh()` 里 `wb` → `scanner.scan_full()`、`dsh` → `load_dsh_stats()`，**两个都只读本机**；
  只有 `multi` 分支走了 `peer_store.merge_machines` → 所以只有多机页看得到别机数据。

**二、改动**
- `peer_store` 新增 **`source_view(entries, source_key, …)`**：单来源合并视图，结构与
  `scan_full()`/`load_dsh_stats()` **完全同构**（`daily`/`models`/`dailySessions`/`sessionsTotal`/
  `today`/`firstDay`/`lastDay`），页面拿到直接渲染。两处刻意的取舍：
  · `today` 由**合并后的 `daily[今天]` 现算** —— 不照抄别机快照里的 `today`
    （那是它**导出那天**的值，隔天导入就会算错日期）；
  · `today.cost` **只取本机** —— 费用是 DSH 本地账本字段，无法得知别机"今天"花多少。
  另新增 `totalCost`（各机累计花费相加）与 `machinesUsed` / `mergedPeers`（供副标题标注构成）。
- `card_app` 新增 **`CardWindow._build_source_stats(src_key)`**：本机实时 + 各别机快照 → `source_view`。
  ⚠️ **没有任何别机时走原路径**（直接返回扫描原始结构）—— 零别机时行为与加这个功能前**完全一致**，
  不给既有回归留隐患。
- `refresh()` 的 `wb`/`dsh` 分支改走它；`load_initial()` 的 `dsh` 分支同改，
  `wb` 首屏改为"**磁盘缓存 + 别机**"合并（否则会先显示纯本机数字、100ms 后台刷新回来再**跳变**）。
- 副标题标注数据构成：`WorkBuddy · … · 含 1 台别机 · 已更新 HH:MM:SS`（DSH 页同理，放在累计花费之后）
  —— 页面上的数字"来路不明"比数字小更糟。

**三、验证**
- 回归 **268 → 282 项全绿**（新增 §15 共 14 项：结构同构 / 来源隔离 / `dsh` 标记 / `totalCost` 累加 /
  `today` 现算不照抄 / 历史日期保留 / 无别机走原路径 / 有别机合并 / 副标题标注）。
- 真实数据端到端（你已导入的「办公室」）：
  · WB **7,630,663,333 → 7,785,301,766**（+154,638,433，与导入包登记的 `total_tokens` **完全一致**）
  · DSH 10,447,610,606 → 10,601,157,186；累计花费 347.65 → 350.55
  · 机器构成 `['LikC', '办公室']`，日期跨度 07-22 ~ 09-19
- `tools/syntax_check.py` 10/10；已重启应用。

**四、一个容易看错的坑（记录备查）**
`load_dsh_stats()` **不返回 `models`**（models 要现算）—— 验证脚本若用 `s["models"]` 求和会得到 **0**，
从而误判"本机 DSH 没数据"。这正是 `peer_store` 里反复强调要走 `normalize_source` 的原因。
（本机 DSH 真实量是 **104 亿**，不是 0。）

---

### 2026-09-19 浅浅猫 × WorkBuddy（第56轮：接入 GitHub 远程仓库）

> 浅猫："已配置好 GitHub 连接器，将我们的 token 统计工具项目推送到 GitHub 仓库，以便在两台机器之间协同开发。"

**一、目标与现状**

| 项 | 值 |
| --- | --- |
| 远程仓库 | `https://github.com/Suqianqiana/token-stats.git`（**private**） |
| 当前分支 | **`main`**（`origin/main`，首次推送用 `-u` 建立跟踪） |
| 已配置 | `origin` 已加、提交身份已设、`.gitignore` 已补齐、`推送到GitHub.bat` 已就位 |
| **待完成** | ⚠️ 仓库尚未创建 + 首次授权需在本机双击脚本（见第四节） |

**二、连接器权限实测（关键发现，省得以后再试）**
- GitHub 连接器（MCP）**只读**：`create_repository` 返回 **403 `Resource not accessible by integration`**
  → 无法替用户创建仓库。
- `push_files` 只能做"单次 API 提交"、**不保留 git 历史**；本项目有 56 轮提交历史必须保留，
  所以远程推送**只能走本地 `git push`**。
- `connect_cloud_service` 明确限定于多模态 / 金融 / 资料库场景，**本场景未使用**（不用它取凭据）。
- **本地无 GitHub 凭据**（Windows 凭据管理器无 github 条目、无 token 环境变量）；
  且 WorkBuddy 执行环境**没有终端/GUI**，Git Credential Manager 弹不出登录窗口 ——
  实测报错：`bash: /dev/tty: No such device or address` →
  `fatal: could not read Username for 'https://github.com'`（exit 128）。

**三、推送前的准备（已完成）**
- **敏感信息扫描**（推送前必做）：项目内**无**硬编码口令/密钥/cookie、**无**凭据文件
  （商汤 token / login_state / autosync / settings / peers 等全部只存在于 `~/.workbuddy` 插件数据目录）。
- `.gitignore` 补齐四类（提交 `d4a0dd2`）：
  ① **敏感文件防误放**：`sn_token.json` / `sn_login_state.json` / `sn_account.json` / `sn_autosync*.json` /
     `sn_points_sync.json` / `machine.json` / `settings.json` / `stats.json` / `peers/` /
     `*_ledger*.json` / `*.pem` / `*.key` / `*.p12` / `.env`；
  ② **依赖目录**：`.venv/` / `venv/` / `node_modules/` / `*.egg-info/`；
  ③ **测试检查缓存**：`.pytest_cache/` / `.mypy_cache/` / `.ruff_cache/` / `.coverage`；
  ④ **编辑器与系统**：`.idea/` / `.vscode/` / `Thumbs.db` / `desktop.ini` / `.DS_Store`。
  原有 `/_archive/`、质检 JSON、字节码、日志、`build/`/`dist/`/`*.spec` 保持不变。
- **提交身份**改为 GitHub noreply 邮箱 `318013379+Suqianqiana@users.noreply.github.com`
  （提交可关联到账号、且不暴露真实邮箱）。
- ⚠️ **历史提交作者一律不动** —— 重写历史会让另一台机器的 commit hash 全部改变，
  直接破坏第54轮建立起来的"同一 commit 就是同一历史"同步基准。
- 新增 `推送到GitHub.bat`（双击 → 弹一次授权 → 自动 `git push -u origin main`）。

**四、需要浅猫做的两步（约 30 秒）**
1. 打开 https://github.com/new → 仓库名 **`token-stats`** → 选 **Private** →
   **不要勾 "Add a README file"**（必须保持空仓库，否则首次 push 会因历史无关而冲突）→ Create。
2. 双击项目根目录的 **`推送到GitHub.bat`** → 在弹出的授权窗口里用 GitHub 账号确认 →
   看到 `[OK] Push succeeded` 即完成。授权一次后凭据进 Windows 凭据管理器，之后直接 `git push` 即可。

**五、两台机器协同的两条硬规矩（重要）**
1. **只在 `main` 上协作，禁止 rebase / amend 已推送的提交** —— 一旦重写历史，另一台机器的
   commit hash 全部对不上，只能强推，风险极高（第54轮正是靠"同一 commit hash"才确认为同源）。
   同步方式：`git pull --rebase=false`（即默认 merge）或先 `git fetch` 看清差异再合并。
2. **`_archive/` 永远不会随 git 同步**（它在 `.gitignore` 里）—— 归档区是"本机磁盘整理"的产物。
   跨机同步若需要归档内容，必须单独拷贝，别指望 git（第54轮已踩过一次坑）。
3. 每台机器各自的本机数据（`~/.workbuddy/plugins/data/token-usage-stats/`）**也不在仓库里**，
   那是运行期数据；跨机共享用量请走程序内的「导出/导入别机数据」，不要塞进仓库。

**六、推送完成（2026-09-19 09:24，首次推送成功）**

- 第一次推送报 `403 Permission to Suqianqiana/token-stats.git denied` ——
  **诊断结论：token 有效、可读、不可写**（读：`/user/repos` 返回 200 且列表里有 `token-stats`；
  写：往仓库 PUT 一个探针文件返回 **403**）。→ 根因是 fine-grained PAT 缺 **Contents: Read and write**。
  浅猫改权限后（**编辑权限不会改变 token 值**）重推即成功。
- **结果**：`* [new branch] main -> main`，耗时 **4 分 36 秒**（88 MB）。
  校验：本地 `main` 与远程 `refs/heads/main` **hash 完全一致**（`ccc442d`）—— 73 个提交历史完整。
- **沙箱限制（记录备查，以后同样场景少走弯路）**：
  · `git push` 这种长耗时命令前台会被中断（88 MB 上传），**必须用后台执行**；
  · `refs/remotes/*` **写不进去**（`git fetch` 与 `git update-ref` 都失败）→ 上游跟踪改用配置方式：
    `git config branch.main.remote origin` + `git config branch.main.merge refs/heads/main`；
  · `git credential approve`（经 helper-selector）**存不进**凭据；但**直接调用**
    `git-credential-manager.exe store` 可以（rc=0）—— 之后 `git ls-remote origin main` **免 token** 成功。
- ⚠️ **仓库当前是 public**。如需私有：GitHub → Settings → General → Danger Zone → Change visibility。
- **凭据已进 Windows 凭据管理器**：两台机器各自存一次即可，之后 `git push` / `git pull` 都不用再给 token。

**七、另一台机器接入方式**
```bash
git clone https://github.com/Suqianqiana/token-stats.git
cd token-stats
# 首次推送时按提示授权一次（或用 PAT 调 git-credential-manager.exe store 存起来）
```
- 记得把 `git config user.name` / `user.email` 设成同样身份（noreply 邮箱），提交才能关联到账号。
- **开工先 `git pull`，收工 `git push`**；两边都在 `main` 上走，不要各开一支。

---

### 2026-09-20 浅浅猫 × WorkBuddy（第58轮：商汤积分卡常驻 + 多机框描边对齐 + README 重写）

**零、本轮诉求（原文）**
> "第一次打开程序的时候商汤页面上面两个积分卡会不显示，而是显示一段文字，要等加载好凭证才会出现积分卡，
>  我认为可以改成不管怎样积分卡都固定在那里，只是没加载凭证的时候数据是默认满额数据，然后那行提示词可以挪到最底部；
>  接着就是继续优化多机合并页，多机名字的框框和输入时候的框框边缘都有点不清晰，没有商汤页输密码那个框框清晰，需要优化；
>  完成之后，这份 readme 写的不够规范与详细准确我觉得，可以根据最新规范重新起草再写一份推上去吗？"

**一、商汤页：两张积分池卡改为常驻 + 未就绪用默认满额 + 提示行下移**
- **定位**：`pools` 在正常路径下**永远有 3 条**（general / flash_lite / promo），所以"只显示一段文字"
  只发生在**首帧缓存为空**时 —— `load_initial()` 用 `self._sn_cache or {"source":"sn","pools":[]}`，
  `SNQuotaPage.render()` 命中 `if not pools` 分支，把整个卡片区换成一段提示文字。
- **改法**：新增模块级 `_default_sn_pools()`，产出与 `load_sn_stats().build_pool()` **完全同形**的占位池
  （唯一区别是 `remaining == total`，即还没消耗）；重置时间取**真实的**下一次 5h 窗口边界
  （`sn_window_bounds()`）与滚动周锚点（`_next_weekly_reset_str()`），避免出现 "—" 的空洞感。
  `render()` 改为「缺哪张补哪张」，两张卡**无条件**渲染。
- **提示行**：原空态文案（"未检测到商汤模型调用记录…"）改为页面底部的 `hint_lbl`，
  位置在同步面板之后、脚注之前；**占位时显示、数据到齐自动隐藏**；文案同步修正
  （原写"可在**下方**配置 cURL"，但同步面板在提示之上 → 改为"在「商汤同步」面板粘贴官网 cURL"）。

**二、多机页：机器框 / 内联输入框描边对齐商汤页输入框**
- **务必先看图再动手**：用 offscreen 渲染把「多机静态框 / 多机编辑框 / 商汤输入框」并排后放大 **4 倍**对照，
  才看清差异 —— 旧实现在 `MachineBox._paint_shell()` 里用 `CARD`（近白/近黑）alpha 150~190 填充
  + `BLUE` 描边 **alpha 仅 90/120**，填充与所在玻璃卡片**几乎同色（零对比）**、描边又是低透明度淡蓝，
  于是只剩一条若隐若现的线。
- **商汤输入框为什么清晰**：它靠的是 **`TRACK` 填充与卡片底形成明度差** + `BORDER` 细描边 ——
  **不是**靠粗 / 艳的描边。找到这一点，修复方向就唯一了。
- **改法**：`_paint_shell()` → `TRACK` 填充 + `BORDER` 全不透明 1px 描边；
  内联输入框边框由「常亮蓝」改为 `BORDER` 细线 + `:focus` 转蓝（padding `1px 7px` → `2px 8px`），
  与商汤输入框成一套语言。修复后再放大对照，新框与商汤框已同源。

**三、回归测试 282 → 289（新增 7 条，全绿）**
- 商汤页：空 pools 仍渲染 2 张池卡 / 占位卡为默认满额（remaining == total）/ 占位态底部提示可见 /
  数据到齐提示自动隐藏 / 数据到齐池卡仍 2 张 / `_default_sn_pools()` 覆盖两池且与真实池同形。
- 多机页：机器框描边**走全局 `BORDER` 令牌**（防止日后被改回硬编码淡蓝而重新变糊）。
- ⚠️ 顺带纠正：本文件与 README 原先分别写 192 项 / 268 项，**都不对**；测试用的是 `check()` 动态累加器，
  实测**当时**为 282 项，本轮加 7 条 → **289 项**。凡引用断言数一律以此为准。

**四、README 重写（工程详尽版 · 纯中文 · 含预览截图）**
- 按浅猫确认的方向重写：**工程详尽版**（架构 / 模块职责 / 数据流 / 数据文件格式 / 开发调试 /
  视觉系统 / 协作规范全覆盖）+ 新增 **5 张预览截图**（offscreen 渲染，**合成匿名数据**，
  不暴露真实用量与账号；桌宠复用既有 `pet_mode.png`）。
- 补齐了旧版缺失的**真实格式细节**：`settings.json` 键清单、导出包 `token-stats.peer/1`、
  `peers/index.json` 与 `machine.json` 布局、商汤口径常量（5h 分块 / 60,000 / 600,000 / JWT 3h /
  TTL 300s / 凭据域名校验）、`SIZE_METRICS`（990×610 / 1180×730）与 `GLASS_PRESETS` 三档等。
- 结构：目录 → 简介 → 界面预览 → 功能详解 → 技术架构 → 数据与文件格式 → 环境 → 启动 → 商汤配置 →
  打包发布 → 开发调试 → 代码规范与视觉系统 → GitHub 协作 → 目录结构 → 已知限制 → 附录。

**五、⭐ offscreen 截图踩坑（已写进 README §10.3，下次直接照做）**
1. **`processEvents()` 默认不处理 DeferredDelete**：`render()` 里旧控件走 `deleteLater()`，
   旧控件从布局摘除后**仍是子控件、留在原位继续绘制** → 会画成"表头与首行重叠"的**假象**，
   极易误判成 UI bug。截图前需 `QCoreApplication.sendPostedEvents(None, QEvent.DeferredDelete)`。
2. `StackedBarChart` 的 `view_start` / `view_count` 有"末日期未变则保留滚轮缩放/平移"逻辑
   → 截图前需显式复位到全览，否则会截到"只有 5 天"的图。
3. `widget.grab()` 对透明窗口**保留 alpha**（角落是 `rgba(0,0,0,0)`），可直接叠到自选背板；
   若发现角落被合成成实色，先查 alpha 是不是被自己覆盖掉了。
4. 区间按钮要与数据一致：应调 `_switch_range(w.btn_30)` 而非直接赋值 `w.range`，否则按钮高亮与数据不符。

**六、⚠️ 待浅猫确认的一处不一致**
- `card_app.py` 头部 docstring 仍写 **"Token 统计卡片 V8.7"**，而同文件 `APP_BUILD = "v9.1-pet-v4"`
  —— 两个版本标识不一致（docstring 明显滞后）。本轮 README 采用运行期常量 `v9.1-pet-v4`；
  是否把 docstring 一并改为一致，等浅猫定夺（未擅自改动）。
  （**已在第59轮解决**：浅猫确认统一 → docstring 改 V9.2、`APP_BUILD` 改 `v9.2-multi`。）

---

### 2026-09-20 浅浅猫 × WorkBuddy（第59轮：按机统计双栏+环形占比圈、版本统一 V9.2、README 桌宠换 V3）

**零、本轮诉求（原文）**
> "版本标识可以统一，不过现在9.1版本也不是最新的了，你更新成9.2并描述新特性（多机同步，用英文），
>  不过在此之前，先进行这一轮对多机页的改进，按机统计那个卡片我希望可以改成双栏的，中间用细线隔开，
>  左边还是之前的呈现方式，右边是环形占比圈，然后按机统计也可以切换今日、总量；
>  然后 readme 的桌宠素材需要放 V3 的素材"

**一、多机页「按机统计」改双栏 + 环形占比圈 + 今日/总量切换**
- **数据层先补前提**：`peer_store.machine_summary()` 新增 `today` 字段（按**当天日期**从规范化后的
  daily 现取；别机当天没同步即 0，口径诚实）。原结构只有全程 `total/requests`，撑不起口径切换。
- **`StatRankRow` 改双栏**：左栏**原封不动**沿用第51轮呈现（名次徽标 / 机器名+本机圆点 / 数值 /
  占比条 / 请求·区间），可用宽度收窄为 `w - RANK_COL["ring_w"]`；中间一条 `BORDER` 细竖线；
  右栏新增 `_paint_ring()` —— `drawArc` 画"底环 TRACK + 前景弧(本机蓝/别机紫)"，圆心写百分比。
  · Qt 的 `drawArc` 角度以 3 点方向为 0°、**逆时针为正** → 起点取 90°(12 点)、跨度取负值才是"从顶部顺时针"。
  · 满圈 360° 会被 Qt 当成"不画"，故把跨度夹到 359.9°(=5759/16)。
  · ⭐ **两条"占比"口径刻意不同且互补**：左栏条按**最大机器**归一（看排行），右栏环按**合计**求真占比（看份额）。
- **口径切换**：面板头右侧加 **今日 / 总量** 分段按钮（样式复用商汤同步面板那套：选中实心蓝 / 未选中
  TRACK+BORDER），切换只重画统计列表（`_render_stat_rows()`），不重跑整页；今日全 0 时给空态而非一排 0。
- `_panel_head()` 加 `extra=` 参数（在右侧说明**之前**插入控件），省得各页各自拼 layout。

**二、⭐ 花了整轮才定位的崩溃：属性名撞 Qt 虚函数**
- 现象：改完必崩 `exit 139`（segfault），且 **stdout 全空**（重定向后块缓冲，崩溃时丢输出）。
- 排查路径（值得记）：`faulthandler` 只打出 `processEvents()` 的 Python 栈 → 判断崩在纯 C++ 路径；
  monkeypatch 逐块屏蔽（`_paint_ring` / `paintEvent` / 列头 / 分段样式）→ 锁定 `StatRankRow.paintEvent`；
  再把"真实 paintEvent"用 `try/except` 包一层 → **真凶现形**：
  `TypeError: Error calling Python override of QWidget::metric(): 'str' object is not callable`。
- **根因**：`QWidget` 继承 `QPaintDevice`，`QPaintDevice::metric()` 是**虚函数**；我把口径存成
  `self.metric = "total"`，PySide6 在构造 `QPainter(self)` 时按虚函数去调 `self.metric` → 撞成字符串 →
  抛异常 → **进程直接 abort**（不是可捕获的普通异常）。
- 修复：改名 `self.stat_metric`；测试里加**守门断言** `callable(_r59.metric)` 防复发。
  **教训**：Python 侧别用 Qt 虚函数名做实例属性（`metric` / `event` / `sizeHint` / `paintEvent`…）；
  覆写 Qt 虚函数的方法里一旦抛异常就是硬崩溃，排查先给覆写包 try/except。

**三、版本标识统一到 V9.2 + 英文新特性**
- docstring `V8.7` → **`V9.2`**（第58轮遗留的不一致，本轮浅猫确认统一）；
  `APP_BUILD` `v9.1-pet-v4` → **`v9.2-multi`**；docstring 内新增英文 `V9.2 highlights (Multi-Machine Sync)` 段。
- README 新增英文 **`What's New in v9.2`** 段（多机同步为主线：导出→导入→合并、非破坏性、一键两语义、
  各页都合并、按机统计双栏+环形+今日/总量、商汤卡常驻）—— 按浅猫要求**用英文**描述新特性。

**四、README 桌宠素材换成 V3**
- 原先引用旧 `pet_mode.png`（早期素材）。本轮用 `BallWindow.set_pet(True)` 在 `pet_theme="v3"`
  （= `assets/pet_v3r`）下渲染，落 `docs/assets/previews/pet_v3.png`（118×148 上采样 3×）。
  ⚠️ `set_pet()` 内部会 `save_settings()` → **必须先把 `SETTINGS_FILE` 指向 tempfile**，否则污染真机配置。
- 多机页预览图 `04-multi-machine.png` 同步换成新双栏设计，新增 `06-multi-today.png`（今日口径）。

**五、回归 289 → 301（全绿）**
- 新增 12 条：`metric` 未被覆盖（守门）/ 口径存于 `stat_metric` / share 对合计求 / today 口径取值与占比 /
  `RANK_COL["ring_w"]` / 具备 `_paint_ring` / 分段按钮与默认口径 / `machine_summary` 的 today 与 total
  不混淆 / 切今日文案变「今日合计」/ 今日全 0 给空态 / 切回总量恢复 2 行。
- ⚠️ 附属发现：`machine_summary` 的 `total` 是**从 `models` 聚合**的（`daily` 不反推）→ 写 fixture 时
  两份数据要自洽，否则 total 会静默为 0（我第一版 fixture 就踩了，被断言拦下）。

---

### 2026-09-22 浅浅猫 × WorkBuddy（第60轮：workdaddy 副本去重 —— 修 token 翻倍）

**零、本轮诉求（原文）**
> "我安装了 workdaddy 这个项目，是为了在一个 workbuddy 里同时用两个账号，但是会话是同一份，
>  它的机制是复制一份会话数据，好像我们这个项目检测到的 token 翻倍了，
>  我希望可以只检测统计我们原本会话的 token，不要多复制一份"

**一、根因（先分析后动手）**
- workdaddy（同账号多开）为第二个账号**复制**会话数据：生成**新 UUID 文件名**，但文件内部
  `sessionId` 字段**仍是原值**。scanner 按「文件路径」遍历 `projects/**/*.jsonl`，
  于是同一份会话被当成两个独立会话统计 → **token 翻倍**。

**二、取证（419 个 jsonl 全量核对）**
- 按内部 sessionId 分组 → **11 组重复**（每组 2 份）；实测每个副本与原件**内容哈希一致**。
- ⭐ **关键反直觉发现**：判据不能只用"文件名==sid"。有 2 组两文件**大小不同**（如 45.93MB vs 20.28MB），
  原因是**复制后两个账号各自继续写入** → "文件名≠sid"那份反而更完整（差 1106 条消息）。
  若只按文件名判断会**丢掉真实 token**。
- 量化：全量 total **108.05 亿** → 去重后 **80.58 亿**（虚增 **25.4%**）。

**三、实现（扫描侧过滤）**
- `scanner.py` 新增 `_read_session_id()`（读首部 40 行正则取 sid，避免为取一个字段 json.loads 大行）
  与 `_dedupe_files()`：同 sid 保留**最完整**那份 —— ①文件更大者优先 ②同大小则"文件名==sessionId"优先；
  **无 sid 的文件一律保留**（宁多勿漏）。
- `scan_full()` 扫描前先做去重决策，循环改遍历 `keep_files`；`{path: sid}` 缓存进 `cache.json`
  的 `__sids__`（复用增量落盘，避免每次重读首部）；result 新增 `dupFilesDropped/dupFilesTotal` 可观测字段。
- `card_app._sn_events_all()`（商汤事件统计）**同一批 jsonl 也会翻倍**，一并接入同一去重
  （实测副本里虚增 213 个商汤事件）。

**四、验证**
- 单元：完整性(keep+drop==全部) / keep 无重复 sid / 丢弃项在 keep 有代表 / 缓存复跑一致 —— 全过。
- 端到端（force 全量重扫 21.5s）：**total 10804.7M → 8057.5M、entries 56349 → 45877、丢弃 11/419**
  —— 与独立分析目标**分毫不差**；sessions 381 未受影响。
- 新增 §16 副本去重测试 10 条（同大小取 sid 名 / 大小不同取更大 / 无 sid 保留 / 完整性 /
  scan_full 端到端条目数与 input / 缓存复跑一致），**回归 301 → 311 全绿**。

**五、教训**
- 判"重复"要看**内容归属**（内部 sessionId）而非**文件名**；文件大小差异恰恰暴露了"复制后各自续写"的真实语义。
- 扫描侧统一过滤（scanner + 商汤事件），避免"数据源一处修、展示处还翻倍"。
